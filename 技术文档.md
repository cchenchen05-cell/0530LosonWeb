# 技术文档

## 1. 项目概述

本项目是一个完整的公司官网系统，包含前台展示网站和后台管理系统（CMS），采用前后端分离架构。

## 2. 技术架构

### 2.1 整体架构
- **Monorepo 结构**：frontend/（前台）+ admin/（后台管理）+ backend/（后端）
- **前端**：Vite + React 19 + TypeScript + Tailwind CSS + React Router
- **后端**：Express.js + TypeScript + SQLite (sql.js)
- **认证**：JWT Token
- **数据验证**：Zod Schema
- **密码加密**：bcryptjs

### 2.2 项目结构
```
/workspace/
├── backend/          # 后端服务
│   ├── src/
│   │   ├── db/       # 数据库层（sql.js + 初始化/种子）
│   │   ├── routes/   # API 路由（auth, products, categories, partners, banners, contact, users）
│   │   ├── middleware/ # 认证中间件
│   │   ├── utils/    # Zod 验证器
│   │   └── index.ts  # 入口
│   └── .env          # 环境变量
├── frontend/         # 前台展示网站
│   └── src/
│       ├── api/      # API 客户端
│       ├── components/ # 组件（Header, Footer, BannerCarousel, CompanyIntro, ProductCenter, Partners, ContactUs, ProductCard）
│       ├── pages/    # 页面（Home, About, Products）
│       ├── lib/      # 工具函数
│       └── App.tsx   # 路由
└── admin/            # 后台管理系统
    └── src/
        ├── api/      # API 客户端
        ├── context/  # AuthContext
        ├── components/ # AdminLayout, DataTable, FormDialog, ConfirmDialog, ProtectedRoute
        ├── pages/    # Login, Dashboard, Products, Categories, Partners, Banners, Users, ContactInfo
        └── App.tsx   # 路由
```

## 3. 核心页面/模块说明

### 3.1 前台模块
| 模块 | 文件 | 说明 |
|------|------|------|
| 导航栏 | Header.tsx | 固定顶部导航，含 Logo、导航链接（首页、产品中心、关于我们、联系我们）、搜索框、登录按钮。支持移动端汉堡菜单 |
| 轮播图 | BannerCarousel.tsx | 3 秒自动播放，淡入淡出过渡，支持手动箭头切换和底部指示点 |
| 公司简介 | CompanyIntro.tsx | 图片+文字左右布局，包含数据统计（产品数量、合作伙伴、年服务等） |
| 产品中心 | ProductCenter.tsx | Tab 分类切换，搜索筛选，产品卡片网格（3 列），分页功能，点击显示详情弹窗 |
| 合作伙伴 | Partners.tsx | 8 个合作伙伴 Logo 网格展示 |
| 联系我们 | ContactUs.tsx | 展示电话、邮箱、地址（来自 API），嵌入地图 |
| 底部信息 | Footer.tsx | 公司信息、快速链接、社交媒体图标、版权声明 |

### 3.2 后台模块
| 模块 | 文件 | 说明 |
|------|------|------|
| 登录页 | LoginPage.tsx | 用户名+密码登录，错误提示 |
| 仪表盘 | DashboardPage.tsx | 统计卡片（产品数、分类数、合作伙伴数、轮播图数） |
| 产品管理 | ProductsPage.tsx | CRUD + 分页 |
| 分类管理 | CategoriesPage.tsx | CRUD |
| 合作伙伴 | PartnersPage.tsx | CRUD + 排序 |
| 轮播图管理 | BannersPage.tsx | CRUD + 图片预览 + 排序 |
| 用户管理 | UsersPage.tsx | CRUD + 角色分配（admin/editor） |
| 联系信息 | ContactInfoPage.tsx | 编辑表单 |

## 4. 数据流转逻辑

### 4.1 前台数据流
```
浏览器请求
  → GET /api/products (分页 + 搜索 + 分类)
  → GET /api/categories
  → GET /api/partners
  → GET /api/banners
  → GET /api/contact-info
  → SQLite 数据库读取
  → 返回 JSON
  → React 组件渲染
```

### 4.2 后台数据流（带认证）
```
登录：POST /api/auth/login → bcrypt 验证 → JWT 签发 → 返回 token
请求：前端附加 Authorization: Bearer <token> → 后端 authMiddleware 验证 JWT → 数据库操作 → 返回结果
```

### 4.3 认证流程
```
1. 用户输入用户名/密码 → POST /api/auth/login
2. 后端 bcrypt.compare 验证密码
3. 验证通过 → jwt.sign() 生成 token
4. 前端 localStorage 存储 token
5. 后续请求 axios 自动附加 Authorization header
6. 后端 authMiddleware 验证 token → 允许访问 / 拒绝访问 (401)
```

## 5. 前后台功能对应关系

| 前台展示 | 后台管理 | 数据源 |
|----------|----------|--------|
| 首页轮播图 | 轮播图管理（增删改查+排序） | banners 表 |
| 产品中心列表 | 产品管理（增删改查+分页+搜索） | products 表 |
| 产品分类 Tab | 分类管理（增删改查） | categories 表 |
| 合作伙伴 Logo | 合作伙伴管理（增删改查+排序） | partners 表 |
| 联系信息展示 | 联系信息配置 | contact_info 表 |
| - | 用户管理（增删改查+角色分配） | users 表 |

## 6. 五个维度分析与改进措施

### 6.1 规范
**现状**：
- 代码风格统一（ESLint + TypeScript 严格模式）
- 目录结构清晰（Monorepo 按功能划分）
- API 设计遵循 RESTful 规范
- 数据库字段命名一致（snake_case）
- 前端组件命名使用 PascalCase，文件命名使用 PascalCase

**改进**：
- 可添加 Prettier 格式化配置
- 可添加 Git pre-commit hooks 进行代码检查
- 可引入 API 文档生成工具（如 Swagger/OpenAPI）

### 6.2 加载速度
**现状**：
- 前端使用 Vite 构建，天然支持代码分割
- Tailwind CSS 按需生成，CSS 体积小（~35KB gzip）
- 前端 JS 包 ~342KB (gzip 111KB) / 后台 ~464KB (gzip 142KB)
- 使用图片懒加载（loading="lazy"）
- 路由级别的代码分割（React.lazy）

**改进**：
- 可配置 CDN 加速静态资源
- 可实现图片 WebP 格式转换
- 可添加 Service Worker 缓存策略
- 可压缩后端响应（compression middleware）

### 6.3 请求时机
**现状**：
- 各页面按需加载数据，无冗余请求
- 搜索功能可防抖处理（debounce）
- 分页减少单次数据量
- axios 实例统一配置 base URL

**改进**：
- 可添加请求缓存（SWR/React Query）
- 可实现接口请求合并
- 可添加重试机制

### 6.4 安全
**现状**：
- 密码 bcrypt 加密存储（salt rounds = 10）
- JWT Token 认证，24 小时过期
- 管理操作需 JWT 验证（authMiddleware）
- 角色权限控制（admin/editor）
- Zod Schema 输入校验
- 参数化 SQL 查询（防 SQL 注入）
- 前端渲染时自动转义（React 默认行为，防 XSS）
- CORS 配置限制来源

**改进**：
- 可添加速率限制（rate limiting）
- 可实现 Token 刷新机制
- 可添加 CSRF Token
- 可实现请求签名验证

### 6.5 数据库负载
**现状**：
- SQLite 作为嵌入式数据库，无网络连接开销
- sql.js 在内存中操作，读写速度快
- 查询使用参数化语句，避免 SQL 注入
- 数据量小（模拟数据）时性能极佳

**改进**：
- 数据量大时可迁移到 PostgreSQL/MySQL
- 可添加查询缓存层（Redis/Memcached）
- 可添加数据库连接池
- 可实现增量备份策略
- 大表可添加复合索引

## 7. 启动方式

```bash
# 后端
cd /workspace/backend && PORT=3001 npx tsx src/index.ts

# 前台
cd /workspace/frontend && npm run dev  # http://localhost:5173

# 后台管理
cd /workspace/admin && npm run dev     # http://localhost:5174
```

默认管理员账号：admin / admin123
默认编辑账号：editor / admin123
