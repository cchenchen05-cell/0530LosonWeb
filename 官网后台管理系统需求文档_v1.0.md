# 电玩设备官网后台管理系统 — 开发需求文档

> 版本：v1.0 | 日期：2026-05-27 | 项目定位：官网前台数据管理 + 前端反馈数据展示

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [系统架构](#3-系统架构)
4. [数据库设计](#4-数据库设计)
5. [后台页面与功能](#5-后台页面与功能)
6. [API 接口设计（后台专用）](#6-api-接口设计后台专用)
7. [文件上传与 OSS 管理](#7-文件上传与-oss-管理)
8. [多语言内容管理](#8-多语言内容管理)
9. [数据统计与反馈展示](#9-数据统计与反馈展示)
10. [权限与认证](#10-权限与认证)
11. [缓存与性能策略](#11-缓存与性能策略)
12. [工程规范](#12-工程规范)

---

## 1. 项目概述

### 1.1 系统定位

后台管理系统是官网前台的数据引擎，核心职责：

| 职责 | 说明 |
|------|------|
| 数据录入 | 管理产品、分类、Banner、内容等前台展示数据 |
| 文件管理 | 上传产品图片、视频、文档到阿里云 OSS |
| 反馈查看 | 查看前台用户提交的询价单、喜欢统计 |
| 内容运营 | 管理客户评价、服务客户、场地案例等运营内容 |
| 多语言管理 | 管理各语言版本的产品和内容数据 |
| 数据统计 | 查看访问量、询价量、热门产品等运营指标 |

### 1.2 与前台的关系

```
┌─────────────┐  上传数据   ┌─────────────┐  API 请求   ┌─────────────┐
│  后台管理系统  │ ──────────→ │   MySQL DB   │ ←────────── │  前台官网    │
│  (Admin CSR) │            │   + Redis    │            │  (SSR/SSG)  │
└─────────────┘            └─────────────┘            └─────────────┘
       ↑                          ↑                          │
       │    询价/喜欢数据写入       │                          │
       └──────────────────────────┘ ←────────────────────────┘
                  后台查看反馈数据        前台用户提交
```

- 后台写入 → 数据库 → 前台读取（展示数据流）
- 前台写入 → 数据库 → 后台读取（反馈数据流）
- 同一个 Next.js 项目，前台用 App Router SSR，后台用 CSR

### 1.3 核心原则

- **同一代码库**：前台和后台共用一个 Next.js 项目，后台路由挂载在 `/admin`
- **前台只读**：前台官网不写数据库（询价提交和喜欢同步除外）
- **后台全权**：所有展示数据由后台录入和管理
- **操作留痕**：所有后台操作记录审计日志
- **软删除**：数据不物理删除，标记 deleted_at

---

## 2. 技术栈

| 体系 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Next.js | 14+ (App Router) | 前台SSR + 后台CSR 同框架 |
| 语言 | TypeScript | 5.x | 类型安全 |
| UI | Tailwind CSS | 3.4 | 原子化 CSS |
| 组件库 | shadcn/ui | 最新 | 后台 UI 组件 |
| 图标 | Lucide React | 最新 | 图标库 |
| 表格 | TanStack Table | 最新 | 后台数据表格 |
| 表单 | React Hook Form + Zod | 最新 | 表单+验证 |
| 富文本 | Tiptap | 最新 | 产品详情编辑 |
| ORM | Prisma | 5.x | 类型安全，防 SQL 注入 |
| 数据库 | MySQL | 8.0+ | 主数据存储 |
| 缓存 | Redis | 7.x | API 缓存 + 会话存储 |
| 认证 | NextAuth | 5.x | 后台登录认证 |
| 文件 | ali-oss | 最新 | 阿里云 OSS SDK |
| 图表 | Recharts | 2.x | 后台数据可视化 |

---

## 3. 系统架构

### 3.1 路由结构

```
src/app/
├── [locale]/                  # 前台路由（SSR）
│   ├── page.tsx               # 首页
│   ├── products/              # 产品
│   ├── about/                 # 关于我们
│   ├── venue/                 # 场地设计
│   └── clients/               # 合作客户
├── admin/                     # 后台路由（CSR）
│   ├── layout.tsx             # 后台布局（侧边栏+顶栏）
│   ├── page.tsx               # 仪表盘
│   ├── login/                 # 登录页
│   ├── products/              # 产品管理
│   ├── categories/            # 分类管理
│   ├── banners/               # Banner管理
│   ├── content/               # 内容管理
│   ├── inquiries/             # 询价管理
│   ├── reviews/               # 评价管理
│   ├── clients/               # 客户管理
│   ├── venues/                # 场地设计管理
│   ├── media/                 # 媒体库
│   ├── languages/             # 多语言管理
│   ├── users/                 # 用户管理
│   └── settings/              # 系统设置
└── api/                       # API 路由
    ├── admin/                 # 后台 API
    └── ...                    # 前台 API（同 v2.0 文档）
```

### 3.2 后台布局

```
┌──────────────────────────────────────────────────────┐
│  [顶栏] Logo | 面包屑 | 通知铃铛 | 管理员头像 ▼       │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│  [侧边栏] │  [内容区域]                                │
│          │                                           │
│  仪表盘   │  搜索/筛选栏                               │
│  产品管理  │  ┌─────────────────────────────────┐     │
│  分类管理  │  │ 数据表格                         │     │
│  Banner  │  │ ...                              │     │
│  内容管理  │  └─────────────────────────────────┘     │
│  询价管理  │  分页器                                    │
│  评价管理  │                                           │
│  客户管理  │                                           │
│  场地设计  │                                           │
│  媒体库   │                                           │
│  ─────── │                                           │
│  多语言   │                                           │
│  用户管理  │                                           │
│  系统设置  │                                           │
└──────────┴───────────────────────────────────────────┘
```

### 3.3 后台页面通用规范

| 项目 | 规范 |
|------|------|
| 列表页 | 搜索 + 筛选 + 表格 + 分页 |
| 表单页 | 分步或分区表单，必填标红星，实时校验 |
| 详情页 | 顶部状态标签 + 信息卡片 + 操作按钮 |
| 删除操作 | 二次确认弹窗 |
| 批量操作 | 勾选后批量删除/上下架 |
| 响应式 | 侧边栏可折叠，表格横向滚动 |

---

## 4. 数据库设计

### 4.1 ER 关系概览

```
users ─────────┐
               │
categories ────┼── products ──── product_images
   │           │      │              │
   └── categories    product_specs   │
      (self-ref)      │              │
                product_translations  │
                       │              │
                product_likes ←──── favorites_sync
                       │
                inquiries ──── inquiry_products
                       │
                reviews
                clients
                venue_designs
                banners
                content_blocks
                audit_logs
```

### 4.2 完整表结构

#### 4.2.1 用户表（users）

```sql
CREATE TABLE users (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  email         VARCHAR(255)   NOT NULL UNIQUE,
  name          VARCHAR(100)   NOT NULL,
  password_hash VARCHAR(255)   NOT NULL,
  avatar_url    VARCHAR(500)   NULL,
  role          ENUM('super_admin', 'admin', 'editor') NOT NULL DEFAULT 'editor',
  status        TINYINT        NOT NULL DEFAULT 1 COMMENT '1-启用 0-禁用',
  last_login_at DATETIME       NULL,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME       NULL,

  INDEX idx_email (email),
  INDEX idx_role (role),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**角色权限**：

| 角色 | 权限 |
|------|------|
| super_admin | 全部权限 + 用户管理 + 系统设置 |
| admin | 全部数据管理权限，不能管理用户和系统设置 |
| editor | 产品/内容/评价管理，不能查看询价和用户管理 |

#### 4.2.2 分类表（categories）

```sql
CREATE TABLE categories (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  parent_id     VARCHAR(36)    NULL COMMENT '父分类ID，NULL为一级分类',
  name_key      VARCHAR(100)   NOT NULL COMMENT '分类标识键（如boxing_machine）',
  icon          VARCHAR(50)    NULL COMMENT '图标标识',
  sort_order    INT            NOT NULL DEFAULT 0 COMMENT '排序，越小越靠前',
  is_hot        TINYINT        NOT NULL DEFAULT 0 COMMENT '1-热门分类 0-非热门',
  status        TINYINT        NOT NULL DEFAULT 1 COMMENT '1-启用 0-禁用',
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME       NULL,

  INDEX idx_parent (parent_id),
  INDEX idx_sort (sort_order),
  INDEX idx_hot (is_hot),

  FOREIGN KEY (parent_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.3 分类翻译表（category_translations）

```sql
CREATE TABLE category_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  category_id   VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL COMMENT '语言代码：zh/en/...',
  name          VARCHAR(200)   NOT NULL,
  image_url     VARCHAR(500)   NULL COMMENT '子分类展示图片URL',

  UNIQUE KEY uk_category_lang (category_id, lang),
  INDEX idx_lang (lang),

  FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.4 产品表（products）

```sql
CREATE TABLE products (
  id                VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  category_id       VARCHAR(36)    NOT NULL COMMENT '一级分类',
  sub_category_id   VARCHAR(36)    NULL COMMENT '二级分类',
  product_key       VARCHAR(100)   NOT NULL UNIQUE COMMENT '产品标识键',
  price             DECIMAL(10,2)  NULL COMMENT '价格，NULL表示面议',
  summary_key       VARCHAR(100)   NULL COMMENT '简介标识键',
  description_key   VARCHAR(100)   NULL COMMENT '详情标识键（富文本翻译表）',
  cover_image_url   VARCHAR(500)   NOT NULL COMMENT '封面图URL',
  base_like_count   INT            NOT NULL DEFAULT 0 COMMENT '基础喜欢数（50-1000随机初始化）',
  actual_like_count INT            NOT NULL DEFAULT 0 COMMENT '实际喜欢数',
  tags              JSON           NULL COMMENT '标签数组 ["hot","new","recommend"]',
  sort_order        INT            NOT NULL DEFAULT 0,
  status            TINYINT        NOT NULL DEFAULT 1 COMMENT '1-上架 0-下架',
  is_featured       TINYINT        NOT NULL DEFAULT 0 COMMENT '1-推荐 0-普通',
  created_at        DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at        DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at        DATETIME       NULL,

  INDEX idx_category (category_id),
  INDEX idx_sub_category (sub_category_id),
  INDEX idx_status (status),
  INDEX idx_sort (sort_order),
  INDEX idx_featured (is_featured),
  INDEX idx_tags ((CAST(tags AS CHAR(500)))) COMMENT 'JSON标签索引',
  INDEX idx_created (created_at),

  FOREIGN KEY (category_id) REFERENCES categories(id),
  FOREIGN KEY (sub_category_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**初始化 base_like_count**：
```sql
-- 产品插入时
SET @base = FLOOR(RAND() * 951) + 50;  -- 50~1000
INSERT INTO products (..., base_like_count) VALUES (..., @base);
```

#### 4.2.5 产品翻译表（product_translations）

```sql
CREATE TABLE product_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  product_id    VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  name          VARCHAR(300)   NOT NULL,
  summary       TEXT           NULL COMMENT '产品简介',
  description   MEDIUMTEXT     NULL COMMENT '产品详情（富文本HTML）',

  UNIQUE KEY uk_product_lang (product_id, lang),
  INDEX idx_lang (lang),

  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.6 产品图片表（product_images）

```sql
CREATE TABLE product_images (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  product_id    VARCHAR(36)    NOT NULL,
  image_url     VARCHAR(500)   NOT NULL,
  alt_text      VARCHAR(200)   NULL COMMENT '图片描述（多语言翻译表）',
  sort_order    INT            NOT NULL DEFAULT 0,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_product (product_id),
  INDEX idx_sort (sort_order),

  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.7 产品规格表（product_specs）

```sql
CREATE TABLE product_specs (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  product_id    VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  spec_key      VARCHAR(100)   NOT NULL COMMENT '规格名：尺寸/重量/功率...',
  spec_value    VARCHAR(500)   NOT NULL COMMENT '规格值',
  sort_order    INT            NOT NULL DEFAULT 0,

  INDEX idx_product_lang (product_id, lang),
  INDEX idx_sort (sort_order),

  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.8 产品喜欢表（product_likes）

```sql
CREATE TABLE product_likes (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  product_id    VARCHAR(36)    NOT NULL,
  visitor_id    VARCHAR(100)   NOT NULL COMMENT '访客标识（浏览器指纹/localStorage ID）',
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,

  UNIQUE KEY uk_product_visitor (product_id, visitor_id),
  INDEX idx_product (product_id),
  INDEX idx_visitor (visitor_id),

  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.9 询价表（inquiries）

```sql
CREATE TABLE inquiries (
  id              VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  inquiry_no      VARCHAR(50)    NOT NULL UNIQUE COMMENT '询价单号',
  name            VARCHAR(100)   NOT NULL,
  phone           VARCHAR(30)    NOT NULL,
  email           VARCHAR(255)   NOT NULL,
  budget          VARCHAR(50)    NULL,
  purchase_time   VARCHAR(50)    NULL,
  address         VARCHAR(500)   NULL,
  status          ENUM('pending', 'processing', 'completed', 'closed') NOT NULL DEFAULT 'pending',
  source          VARCHAR(50)    NOT NULL DEFAULT 'website' COMMENT '来源',
  lang            VARCHAR(10)    NULL COMMENT '提交时语言',
  remark          TEXT           NULL COMMENT '后台备注',
  assigned_to     VARCHAR(36)    NULL COMMENT '分配给',
  created_at      DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_status (status),
  INDEX idx_created (created_at),
  INDEX idx_phone (phone),
  INDEX idx_email (email),

  FOREIGN KEY (assigned_to) REFERENCES users(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.10 询价-产品关联表（inquiry_products）

```sql
CREATE TABLE inquiry_products (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  inquiry_id    VARCHAR(36)    NOT NULL,
  product_id    VARCHAR(36)    NOT NULL,

  UNIQUE KEY uk_inquiry_product (inquiry_id, product_id),
  INDEX idx_inquiry (inquiry_id),
  INDEX idx_product (product_id),

  FOREIGN KEY (inquiry_id) REFERENCES inquiries(id) ON DELETE CASCADE,
  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.11 Banner 表（banners）

```sql
CREATE TABLE banners (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  type          ENUM('video', 'image') NOT NULL DEFAULT 'image' COMMENT 'Hero类型',
  media_url     VARCHAR(500)   NOT NULL COMMENT '视频或图片URL',
  poster_url    VARCHAR(500)   NULL COMMENT '视频封面图URL',
  link_url      VARCHAR(500)   NULL COMMENT '点击跳转链接',
  sort_order    INT            NOT NULL DEFAULT 0,
  status        TINYINT        NOT NULL DEFAULT 1 COMMENT '1-启用 0-禁用',
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME       NULL,

  INDEX idx_sort (sort_order),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.12 Banner 翻译表（banner_translations）

```sql
CREATE TABLE banner_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  banner_id     VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  title         VARCHAR(200)   NULL COMMENT '主标题',
  subtitle      VARCHAR(500)   NULL COMMENT '副标题',
  button_text   VARCHAR(50)    NULL COMMENT '按钮文字',

  UNIQUE KEY uk_banner_lang (banner_id, lang),

  FOREIGN KEY (banner_id) REFERENCES banners(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.13 客户评价表（reviews）

```sql
CREATE TABLE reviews (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  company_name  VARCHAR(200)   NOT NULL,
  avatar_url    VARCHAR(500)   NULL,
  rating        TINYINT        NOT NULL DEFAULT 5 COMMENT '评分1-5',
  sort_order    INT            NOT NULL DEFAULT 0,
  status        TINYINT        NOT NULL DEFAULT 1,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME       NULL,

  INDEX idx_sort (sort_order),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.14 评价翻译表（review_translations）

```sql
CREATE TABLE review_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  review_id     VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  content       TEXT           NOT NULL COMMENT '评价内容',
  reviewer_name VARCHAR(100)   NOT NULL COMMENT '评价人姓名',
  position      VARCHAR(100)   NULL COMMENT '职位',

  UNIQUE KEY uk_review_lang (review_id, lang),

  FOREIGN KEY (review_id) REFERENCES reviews(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.15 客户表（clients）

```sql
CREATE TABLE clients (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  name_key      VARCHAR(100)   NOT NULL COMMENT '客户标识键',
  logo_url      VARCHAR(500)   NOT NULL,
  type          ENUM('served', 'partner') NOT NULL DEFAULT 'served' COMMENT 'served-服务过的 partner-合作',
  sort_order    INT            NOT NULL DEFAULT 0,
  status        TINYINT        NOT NULL DEFAULT 1,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME       NULL,

  INDEX idx_type (type),
  INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.16 客户翻译表（client_translations）

```sql
CREATE TABLE client_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  client_id     VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  name          VARCHAR(200)   NOT NULL COMMENT '客户名称',

  UNIQUE KEY uk_client_lang (client_id, lang),

  FOREIGN KEY (client_id) REFERENCES clients(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.17 场地设计表（venue_designs）

```sql
CREATE TABLE venue_designs (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  cover_image_url VARCHAR(500) NOT NULL,
  sort_order    INT            NOT NULL DEFAULT 0,
  status        TINYINT        NOT NULL DEFAULT 1,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME       NULL,

  INDEX idx_sort (sort_order),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.18 场地设计翻译表（venue_design_translations）

```sql
CREATE TABLE venue_design_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  venue_design_id VARCHAR(36)  NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  title         VARCHAR(200)   NOT NULL,
  description   MEDIUMTEXT     NULL COMMENT '富文本描述',
  area          VARCHAR(50)    NULL COMMENT '面积',
  location      VARCHAR(200)   NULL COMMENT '地点',

  UNIQUE KEY uk_venue_lang (venue_design_id, lang),

  FOREIGN KEY (venue_design_id) REFERENCES venue_designs(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.19 内容区块表（content_blocks）

```sql
CREATE TABLE content_blocks (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  block_key     VARCHAR(100)   NOT NULL UNIQUE COMMENT '区块标识：why_choose_us/suitable_venues/purchase_process/footer',
  sort_order    INT            NOT NULL DEFAULT 0,
  status        TINYINT        NOT NULL DEFAULT 1,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_key (block_key),
  INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.20 内容区块项表（content_block_items）

```sql
CREATE TABLE content_block_items (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  block_id      VARCHAR(36)    NOT NULL,
  icon          VARCHAR(50)    NULL COMMENT '图标标识',
  image_url     VARCHAR(500)   NULL,
  sort_order    INT            NOT NULL DEFAULT 0,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_block (block_id),
  INDEX idx_sort (sort_order),

  FOREIGN KEY (block_id) REFERENCES content_blocks(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.21 内容项翻译表（content_block_item_translations）

```sql
CREATE TABLE content_block_item_translations (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  item_id       VARCHAR(36)    NOT NULL,
  lang          VARCHAR(10)    NOT NULL,
  title         VARCHAR(200)   NOT NULL,
  description   TEXT           NULL,

  UNIQUE KEY uk_item_lang (item_id, lang),

  FOREIGN KEY (item_id) REFERENCES content_block_items(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.22 语言表（languages）

```sql
CREATE TABLE languages (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  code          VARCHAR(10)    NOT NULL UNIQUE COMMENT '语言代码：zh/en/...',
  name          VARCHAR(50)    NOT NULL COMMENT '语言英文名',
  native_name   VARCHAR(50)    NOT NULL COMMENT '语言原生名',
  is_default    TINYINT        NOT NULL DEFAULT 0 COMMENT '1-默认语言',
  sort_order    INT            NOT NULL DEFAULT 0,
  status        TINYINT        NOT NULL DEFAULT 1,

  INDEX idx_code (code),
  INDEX idx_default (is_default)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.23 询价选项表（inquiry_options）

```sql
CREATE TABLE inquiry_options (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  option_group  VARCHAR(50)    NOT NULL COMMENT '选项组：budget/purchase_time',
  lang          VARCHAR(10)    NOT NULL,
  value         VARCHAR(200)   NOT NULL,
  sort_order    INT            NOT NULL DEFAULT 0,

  INDEX idx_group_lang (option_group, lang),
  INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.24 审计日志表（audit_logs）

```sql
CREATE TABLE audit_logs (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  user_id       VARCHAR(36)    NULL,
  action        VARCHAR(50)    NOT NULL COMMENT '操作：create/update/delete/login',
  resource_type VARCHAR(50)    NOT NULL COMMENT '资源类型：product/category/...',
  resource_id   VARCHAR(36)    NULL,
  detail        JSON           NULL COMMENT '变更详情',
  ip_address    VARCHAR(50)    NULL,
  user_agent    VARCHAR(500)   NULL,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_user (user_id),
  INDEX idx_action (action),
  INDEX idx_resource (resource_type, resource_id),
  INDEX idx_created (created_at),

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4.2.25 媒体库表（media）

```sql
CREATE TABLE media (
  id            VARCHAR(36)    PRIMARY KEY DEFAULT (UUID()),
  filename      VARCHAR(255)   NOT NULL COMMENT '原始文件名',
  url           VARCHAR(500)   NOT NULL COMMENT 'OSS URL',
  oss_key       VARCHAR(500)   NOT NULL COMMENT 'OSS Object Key',
  mime_type     VARCHAR(50)    NOT NULL,
  size_bytes    BIGINT         NOT NULL COMMENT '文件大小(字节)',
  width         INT            NULL COMMENT '图片宽度',
  height        INT            NULL COMMENT '图片高度',
  type          ENUM('image', 'video', 'document') NOT NULL DEFAULT 'image',
  alt_text      VARCHAR(200)   NULL,
  folder        VARCHAR(100)   NULL COMMENT '文件夹分类',
  uploaded_by   VARCHAR(36)    NULL,
  created_at    DATETIME       NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_type (type),
  INDEX idx_folder (folder),
  INDEX idx_created (created_at),

  FOREIGN KEY (uploaded_by) REFERENCES users(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.3 多语言设计原则

```
核心思路：主表存结构数据，翻译表存多语言文本

每个需要多语言的实体都有一张 xxx_translations 表：
- 主表：存 ID、排序、状态、图片URL等语言无关数据
- 翻译表：存 lang + 各语言文本字段
- 主从关系：1:N（一条主记录对应多条翻译记录）

查询示例（Prisma）：
const products = await prisma.product.findMany({
  where: { status: 1, deleted_at: null },
  include: {
    translations: { where: { lang: 'zh' } },
    category: { include: { translations: { where: { lang: 'zh' } } } }
  }
});
```

---

## 5. 后台页面与功能

### 5.1 登录页

**路由**：`/admin/login`

| 项目 | 说明 |
|------|------|
| 登录方式 | 邮箱 + 密码 |
| 认证 | NextAuth Credentials Provider |
| Session | JWT，存 httpOnly Cookie |
| 安全 | 登录失败5次锁定15分钟，密码 bcrypt 加密 |
| 记住我 | 7天免登录 |

### 5.2 仪表盘

**路由**：`/admin`

| 数据卡片 | 数据来源 |
|---------|---------|
| 今日询价数 | `SELECT COUNT(*) FROM inquiries WHERE DATE(created_at) = CURDATE()` |
| 待处理询价 | `SELECT COUNT(*) FROM inquiries WHERE status = 'pending'` |
| 产品总数 | `SELECT COUNT(*) FROM products WHERE status=1 AND deleted_at IS NULL` |
| 本月询价数 | `SELECT COUNT(*) FROM inquiries WHERE created_at >= DATE_FORMAT(NOW(), '%Y-%m-01')` |

| 图表 | 说明 |
|------|------|
| 询价趋势 | 近30天每日询价数量折线图 |
| 热门产品 Top10 | 按 actual_like_count 排序 |
| 询价状态分布 | 饼图：待处理/处理中/已完成/已关闭 |
| 最近询价列表 | 最新10条询价简要信息 |

**缓存**：仪表盘数据缓存 5min（询价数实时性要求不高）

### 5.3 产品管理

**路由**：`/admin/products`

#### 列表页

| 功能 | 说明 |
|------|------|
| 搜索 | 按产品名称搜索 |
| 筛选 | 按分类、状态、标签筛选 |
| 排序 | 按排序号、创建时间、喜欢数排序 |
| 批量操作 | 批量上架/下架/删除 |
| 表格列 | 封面图、名称、分类、价格、喜欢数、状态、排序、操作 |

#### 新增/编辑页

**路由**：`/admin/products/new` 或 `/admin/products/[id]`

| 区域 | 字段 | 说明 |
|------|------|------|
| 基本信息 | 分类（一级+二级联动） | 下拉选择 |
| | 价格 | 数字输入，可留空表示"面议" |
| | 标签 | 多选：热门/新品/推荐 |
| | 排序号 | 数字输入 |
| | 状态 | 上架/下架 |
| | 是否推荐 | 开关 |
| 多语言内容 | Tab 切换语言 | 每个语言填：名称、简介、详情（富文本） |
| 产品图片 | 图片上传组件 | 支持拖拽批量上传，可排序 |
| 产品规格 | 动态添加规格行 | 每行：规格名+规格值，按语言分别填写 |
| 封面图 | 单图上传 | 必填 |

**富文本编辑器**（Tiptap）：
- 标题（H2/H3）、段落、加粗、斜体
- 有序/无序列表
- 图片插入（从媒体库选择或上传）
- 表格
- 链接

#### 产品详情查看页

| 区域 | 内容 |
|------|------|
| 产品信息 | 全部字段的只读展示 |
| 喜欢统计 | base_like_count + actual_like_count + 总计 |
| 询价关联 | 包含该产品的询价单列表 |
| 操作日志 | 该产品的操作审计记录 |

### 5.4 分类管理

**路由**：`/admin/categories`

| 功能 | 说明 |
|------|------|
| 树形展示 | 左侧树形展示分类层级 |
| 拖拽排序 | 拖拽调整分类顺序 |
| 新增/编辑 | 弹窗表单：图标、排序、是否热门、各语言名称 |
| 子分类图片 | 每个子分类可上传展示图片 |
| 删除限制 | 有关联产品的分类不可删除，需先移除或转移产品 |

### 5.5 Banner 管理

**路由**：`/admin/banners`

| 功能 | 说明 |
|------|------|
| 列表 | 缩略图 + 标题 + 类型(视频/图片) + 状态 + 排序 |
| 新增/编辑 | 上传视频或图片、各语言标题/副标题/按钮文字、链接、排序 |
| 预览 | 侧边预览 Banner 效果 |
| 视频上传 | 上传到 OSS，自动生成封面图 |

### 5.6 内容管理

**路由**：`/admin/content`

| 区块 | 管理内容 |
|------|---------|
| 为什么选择我们 | 图标+标题+描述，多语言，排序 |
| 产品适合场所 | 图标+标题+描述，多语言，排序 |
| 采购流程 | 步骤图标+标题+描述，多语言，排序 |
| 页脚 | 链接分组+各语言文案 |

操作方式：每个区块为独立卡片列表，支持增删改查和排序。

### 5.7 询价管理

**路由**：`/admin/inquiries`

#### 列表页

| 功能 | 说明 |
|------|------|
| 搜索 | 按姓名、电话、邮箱搜索 |
| 筛选 | 按状态、日期范围筛选 |
| 排序 | 按创建时间倒序（最新在前） |
| 表格列 | 询价号、姓名、电话、产品数、状态、创建时间、操作 |
| 批量操作 | 批量标记处理中/已完成/关闭 |

#### 详情页

**路由**：`/admin/inquiries/[id]`

| 区域 | 内容 |
|------|------|
| 基本信息 | 姓名、电话、邮箱、预算、采购时间、地址 |
| 感兴趣的产品 | 产品列表（图片+名称+价格），可点击跳转产品 |
| 状态流转 | 待处理 → 处理中 → 已完成/已关闭 |
| 分配 | 分配给某个管理员 |
| 备注 | 后台添加跟进备注（时间线） |
| 操作日志 | 审计记录 |

#### 询价状态流转

```
pending (待处理)
    ↓
processing (处理中)
    ↓         ↓
completed (已完成)  closed (已关闭)
```

### 5.8 评价管理

**路由**：`/admin/reviews`

| 功能 | 说明 |
|------|------|
| 列表 | 头像、评价人、公司、评分、状态、排序 |
| 新增/编辑 | 头像上传、评分(1-5星)、各语言：评价人姓名、职位、评价内容 |
| 排序 | 拖拽排序 |
| 上架/下架 | 控制前台是否展示 |

### 5.9 客户管理

**路由**：`/admin/clients`

| 功能 | 说明 |
|------|------|
| 分类 | 按类型筛选：服务过的客户 / 合作客户 |
| 列表 | Logo、名称、类型、状态、排序 |
| 新增/编辑 | Logo上传、类型选择、各语言：客户名称 |
| 排序 | 拖拽排序 |

### 5.10 场地设计管理

**路由**：`/admin/venues`

| 功能 | 说明 |
|------|------|
| 列表 | 封面图、标题、状态、排序 |
| 新增/编辑 | 封面图上传、各语言：标题、描述(富文本)、面积、地点 |
| 排序 | 拖拽排序 |

### 5.11 媒体库

**路由**：`/admin/media`

| 功能 | 说明 |
|------|------|
| 网格视图 | 缩略图网格展示 |
| 列表视图 | 文件名、类型、大小、尺寸、上传时间 |
| 文件夹 | 支持创建文件夹分类（产品图、Banner、Logo 等） |
| 上传 | 拖拽批量上传，自动上传到 OSS |
| 图片处理 | 上传时自动生成缩略图（400px/150px） |
| 删除 | 从 OSS 删除 + 数据库记录删除（软删除） |
| 搜索 | 按文件名搜索 |
| 筛选 | 按类型（图片/视频/文档）、文件夹筛选 |

### 5.12 多语言管理

**路由**：`/admin/languages`

| 功能 | 说明 |
|------|------|
| 语言列表 | 语言代码、名称、原生名、是否默认、状态 |
| 新增语言 | 添加新语言支持 |
| 启用/禁用 | 控制前台是否可选该语言 |
| 设为默认 | 设置默认语言 |
| 翻译进度 | 显示每种语言的翻译完成度（已翻译条目/总条目） |

### 5.13 用户管理

**路由**：`/admin/users`

> 仅 super_admin 可访问

| 功能 | 说明 |
|------|------|
| 列表 | 头像、姓名、邮箱、角色、状态、最后登录 |
| 新增 | 填写姓名、邮箱、初始密码、角色 |
| 编辑 | 修改姓名、角色、状态 |
| 重置密码 | 管理员重置密码 |
| 禁用/启用 | 禁用后不可登录 |

### 5.14 系统设置

**路由**：`/admin/settings`

> 仅 super_admin 可访问

| 设置项 | 说明 |
|--------|------|
| 站点信息 | 站点名称、描述、Logo、Favicon |
| SEO | 默认 Meta 标题/描述/关键词 |
| OSS 配置 | 阿里云 OSS Bucket/Region/CDN 域名（已在环境变量，此处只读展示） |
| 缓存管理 | 手动清除 Redis 缓存 |
| 询价设置 | 询价选项管理（预算范围、采购时间选项） |
| 审计日志 | 查看所有操作日志 |

---

## 6. API 接口设计（后台专用）

> 后台 API 统一前缀 `/api/admin`，需认证。所有写操作需记录审计日志。

### 6.1 认证中间件

```typescript
// 所有 /api/admin/* 路由需通过认证中间件
// 验证 JWT Token
// 从 Token 中提取 user_id 和 role
// 根据 role 检查是否有权访问该接口
// 记录审计日志
```

### 6.2 产品管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 产品列表 | GET | `/api/admin/products` | 后台分页列表，含翻译和统计 |
| 产品详情 | GET | `/api/admin/products/:id` | 完整数据含所有语言翻译 |
| 创建产品 | POST | `/api/admin/products` | 含多语言翻译+规格+图片 |
| 更新产品 | PUT | `/api/admin/products/:id` | 全量更新 |
| 部分更新 | PATCH | `/api/admin/products/:id` | 如上下架、排序 |
| 删除产品 | DELETE | `/api/admin/products/:id` | 软删除 |
| 批量操作 | POST | `/api/admin/products/batch` | 批量上下架/删除 |

**创建产品请求示例**：

```
POST /api/admin/products

Body:
{
  "categoryId": "cat_01",
  "subCategoryId": "sub_01",
  "price": 12800,
  "tags": ["hot", "recommend"],
  "sortOrder": 1,
  "status": 1,
  "isFeatured": true,
  "coverImageUrl": "https://cdn.../cover.webp",
  "translations": [
    { "lang": "zh", "name": "超级拳击机 Pro", "summary": "商用级...", "description": "<p>...</p>" },
    { "lang": "en", "name": "Super Boxing Pro", "summary": "Commercial grade...", "description": "<p>...</p>" }
  ],
  "images": [
    { "url": "https://cdn.../img1.webp", "sortOrder": 1 },
    { "url": "https://cdn.../img2.webp", "sortOrder": 2 }
  ],
  "specs": [
    { "lang": "zh", "specKey": "尺寸", "specValue": "1200×800×2200mm", "sortOrder": 1 },
    { "lang": "en", "specKey": "Dimensions", "specValue": "1200×800×2200mm", "sortOrder": 1 }
  ]
}
```

**数据库优化**：创建时使用 Prisma 事务，一次性写入 products + product_translations + product_images + product_specs 四张表，避免多次连接。

### 6.3 分类管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 分类树 | GET | `/api/admin/categories` | 含翻译的树形结构 |
| 分类详情 | GET | `/api/admin/categories/:id` | |
| 创建分类 | POST | `/api/admin/categories` | 含多语言翻译 |
| 更新分类 | PUT | `/api/admin/categories/:id` | |
| 删除分类 | DELETE | `/api/admin/categories/:id` | 检查关联产品 |
| 拖拽排序 | PATCH | `/api/admin/categories/sort` | 批量更新排序 |

### 6.4 Banner 管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表 | GET | `/api/admin/banners` | |
| 详情 | GET | `/api/admin/banners/:id` | |
| 创建 | POST | `/api/admin/banners` | 含视频/图片+翻译 |
| 更新 | PUT | `/api/admin/banners/:id` | |
| 删除 | DELETE | `/api/admin/banners/:id` | |
| 排序 | PATCH | `/api/admin/banners/sort` | |

### 6.5 询价管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表 | GET | `/api/admin/inquiries` | 含产品关联信息 |
| 详情 | GET | `/api/admin/inquiries/:id` | 含产品列表+备注 |
| 更新状态 | PATCH | `/api/admin/inquiries/:id/status` | 状态流转 |
| 分配 | PATCH | `/api/admin/inquiries/:id/assign` | 分配给管理员 |
| 添加备注 | POST | `/api/admin/inquiries/:id/remarks` | |
| 批量状态 | POST | `/api/admin/inquiries/batch-status` | |
| 导出 | GET | `/api/admin/inquiries/export` | 导出 Excel |

**数据库优化**：
- 列表查询 `status` 和 `created_at` 都有索引
- 详情查询 JOIN inquiry_products + products 一次性获取
- 导出使用流式查询避免内存溢出

### 6.6 评价管理接口

| 接口 | 方法 | 路径 |
|------|------|------|
| 列表 | GET | `/api/admin/reviews` |
| 详情 | GET | `/api/admin/reviews/:id` |
| 创建 | POST | `/api/admin/reviews` |
| 更新 | PUT | `/api/admin/reviews/:id` |
| 删除 | DELETE | `/api/admin/reviews/:id` |
| 排序 | PATCH | `/api/admin/reviews/sort` |

### 6.7 客户管理接口

| 接口 | 方法 | 路径 |
|------|------|------|
| 列表 | GET | `/api/admin/clients` |
| 详情 | GET | `/api/admin/clients/:id` |
| 创建 | POST | `/api/admin/clients` |
| 更新 | PUT | `/api/admin/clients/:id` |
| 删除 | DELETE | `/api/admin/clients/:id` |
| 排序 | PATCH | `/api/admin/clients/sort` |

### 6.8 场地设计管理接口

| 接口 | 方法 | 路径 |
|------|------|------|
| 列表 | GET | `/api/admin/venues` |
| 详情 | GET | `/api/admin/venues/:id` |
| 创建 | POST | `/api/admin/venues` |
| 更新 | PUT | `/api/admin/venues/:id` |
| 删除 | DELETE | `/api/admin/venues/:id` |
| 排序 | PATCH | `/api/admin/venues/sort` |

### 6.9 内容管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 区块列表 | GET | `/api/admin/content-blocks` | |
| 区块详情 | GET | `/api/admin/content-blocks/:key` | 按 block_key |
| 更新区块 | PUT | `/api/admin/content-blocks/:key` | 含 items + translations |
| 新增项 | POST | `/api/admin/content-blocks/:key/items` | |
| 更新项 | PUT | `/api/admin/content-blocks/:key/items/:itemId` | |
| 删除项 | DELETE | `/api/admin/content-blocks/:key/items/:itemId` | |
| 排序 | PATCH | `/api/admin/content-blocks/:key/items/sort` | |

### 6.10 媒体库接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表 | GET | `/api/admin/media` | 按文件夹、类型筛选 |
| 上传 | POST | `/api/admin/media/upload` | multipart/form-data |
| 批量上传 | POST | `/api/admin/media/batch-upload` | 多文件 |
| 详情 | GET | `/api/admin/media/:id` | |
| 更新 | PATCH | `/api/admin/media/:id` | 修改 alt_text、文件夹 |
| 删除 | DELETE | `/api/admin/media/:id` | 同时删除 OSS 文件 |
| 批量删除 | POST | `/api/admin/media/batch-delete` | |
| 创建文件夹 | POST | `/api/admin/media/folders` | |
| OSS 签名 URL | POST | `/api/admin/media/sign-url` | 前端直传 OSS |

### 6.11 用户管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表 | GET | `/api/admin/users` | |
| 详情 | GET | `/api/admin/users/:id` | |
| 创建 | POST | `/api/admin/users` | |
| 更新 | PUT | `/api/admin/users/:id` | |
| 重置密码 | POST | `/api/admin/users/:id/reset-password` | |
| 禁用/启用 | PATCH | `/api/admin/users/:id/status` | |

### 6.12 统计接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 仪表盘概览 | GET | `/api/admin/stats/overview` | 今日/本月询价数等 |
| 询价趋势 | GET | `/api/admin/stats/inquiry-trend?days=30` | 近N天每日询价数 |
| 热门产品 | GET | `/api/admin/stats/top-products?limit=10` | 按喜欢数排序 |
| 询价状态分布 | GET | `/api/admin/stats/inquiry-status` | 各状态数量 |
| 翻译进度 | GET | `/api/admin/stats/translation-progress` | 各语言翻译完成度 |

---

## 7. 文件上传与 OSS 管理

### 7.1 上传流程

```
方式一：服务端中转（小文件）
1. 前端选择文件
2. POST /api/admin/media/upload (multipart/form-data)
3. 后端接收 → 验证文件类型/大小
4. 上传到阿里云 OSS
5. 图片：后端生成缩略图（400px, 150px）
6. 写入 media 表
7. 返回 URL + media ID

方式二：前端直传 OSS（大文件/视频）
1. 前端请求 POST /api/admin/media/sign-url 获取签名
2. 前端直传 OSS（使用签名 URL）
3. 上传完成后 POST /api/admin/media/confirm 通知后端
4. 后端写入 media 表
```

### 7.2 OSS 目录结构

```
bucket/
├── products/                 # 产品图片
│   ├── covers/              # 封面图
│   ├── detail/              # 详情图
│   └── thumbnails/          # 缩略图
├── banners/                  # Banner 资源
│   ├── images/
│   └── videos/
├── clients/                  # 客户 Logo
├── reviews/                  # 评价头像
├── venues/                   # 场地设计图
├── content/                  # 内容区块图
└── common/                   # 通用资源
```

### 7.3 文件限制

| 类型 | 允许格式 | 最大大小 |
|------|---------|---------|
| 图片 | JPG, PNG, WebP, SVG | 10MB |
| 视频 | MP4, WebM | 200MB |
| 文档 | PDF | 20MB |

### 7.4 图片处理

上传图片时后端自动处理：

| 处理 | 说明 |
|------|------|
| 格式转换 | 自动转 WebP（保留原图） |
| 缩略图生成 | 400px 宽（列表用）+ 150px 宽（缩略图用） |
| 元数据提取 | 读取宽高写入 media 表 |
| CDN 分发 | 通过阿里云 CDN 加速 |

---

## 8. 多语言内容管理

### 8.1 管理方式

```
后台每个需要多语言的表单都使用 Tab 切换语言：

┌─────────────────────────────────────┐
│  [中文] [English] [Español]          │  ← 语言 Tab
├─────────────────────────────────────┤
│  产品名称：[超级拳击机 Pro          ] │
│  产品简介：[商用级拳击机...         ] │
│  产品详情：[富文本编辑器...         ] │
└─────────────────────────────────────┘
```

- 默认语言 Tab 高亮，必填
- 其他语言 Tab 可选填
- 未填写的语言，前台回退到默认语言
- 左上角显示翻译状态指示器：✅ 已翻译 / ⚠️ 未翻译

### 8.2 回退机制

```
前端请求 ?lang=es 时：
1. 查询 es 翻译记录
2. 如果 es 翻译存在 → 返回 es
3. 如果 es 翻译不存在 → 回退到默认语言（zh）
4. 回退的字段标记 fallback=true，前端可显示"未翻译"提示
```

### 8.3 翻译进度

后台可查看各语言的翻译完成度：

```
| 语言 | 产品 | 分类 | Banner | 评价 | 场地 | 总进度 |
|------|------|------|--------|------|------|--------|
| zh   | 45/45| 8/8  | 5/5    | 20/20| 10/10| 100%   |
| en   | 42/45| 8/8  | 5/5    | 18/20| 8/10 | 93%    |
| es   | 30/45| 5/8  | 3/5    | 10/20| 5/10 | 64%    |
```

---

## 9. 数据统计与反馈展示

### 9.1 仪表盘统计卡片

| 卡片 | 数据 | SQL | 刷新 |
|------|------|-----|------|
| 今日询价 | `COUNT(*) WHERE DATE(created_at)=CURDATE()` | 索引 idx_created | 5min |
| 待处理询价 | `COUNT(*) WHERE status='pending'` | 索引 idx_status | 5min |
| 产品总数 | `COUNT(*) WHERE status=1 AND deleted_at IS NULL` | 索引 idx_status | 30min |
| 本月询价 | `COUNT(*) WHERE created_at >=月初` | 索引 idx_created | 5min |

### 9.2 询价趋势图

```
近30天每日询价数量折线图

SQL：
SELECT DATE(created_at) as date, COUNT(*) as count
FROM inquiries
WHERE created_at >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
GROUP BY DATE(created_at)
ORDER BY date;

缓存：Redis 5min
```

### 9.3 热门产品 Top10

```
按 likeCount = base_like_count + actual_like_count 排序

SQL：
SELECT p.id, pt.name, p.base_like_count, p.actual_like_count,
       (p.base_like_count + p.actual_like_count) as total_likes
FROM products p
LEFT JOIN product_translations pt ON p.id = pt.product_id AND pt.lang = 'zh'
WHERE p.status = 1 AND p.deleted_at IS NULL
ORDER BY total_likes DESC
LIMIT 10;

缓存：Redis 10min
```

### 9.4 询价详情展示

```
询价详情页展示前端反馈的数据：
1. 客户基本信息（姓名/电话/邮箱/预算/时间/地址）
2. 感兴趣的产品列表
   - 关联查询 inquiry_products + products + product_translations
   - 显示产品图片+名称+价格
3. 询价来源信息（提交时语言、来源页面）
4. 状态流转记录
5. 后台跟进备注
```

### 9.5 喜欢数据

```
产品管理中展示：
- base_like_count：后台初始化的基数
- actual_like_count：实际用户喜欢数
- 总计：base + actual

同步机制：
- 前台 POST /api/favorites/sync 时更新 product_likes 表
- 更新后 actual_like_count = COUNT(*) FROM product_likes WHERE product_id = ?
- 使用 +1/-1 异步更新而非每次 COUNT，减少数据库压力
```

---

## 10. 权限与认证

### 10.1 认证方案

使用 NextAuth v5 + Credentials Provider：

```typescript
// auth.ts
export const authOptions = {
  providers: [
    CredentialsProvider({
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" }
      },
      async authorize(credentials) {
        // 1. 查询 users 表
        // 2. bcrypt 验证密码
        // 3. 检查 status
        // 4. 检查登录失败次数（5次锁定15分钟）
        // 5. 更新 last_login_at
        // 6. 返回 user 对象
      }
    })
  ],
  session: { strategy: "jwt", maxAge: 24 * 60 * 60 },  // 24h
  callbacks: {
    jwt: async ({ token, user }) => {
      if (user) {
        token.role = user.role;
        token.id = user.id;
      }
      return token;
    },
    session: async ({ session, token }) => {
      session.user.role = token.role;
      session.user.id = token.id;
      return session;
    }
  }
};
```

### 10.2 权限矩阵

| 功能 | super_admin | admin | editor |
|------|:-----------:|:-----:|:------:|
| 仪表盘 | ✅ | ✅ | ✅（只读） |
| 产品管理 | ✅ | ✅ | ✅ |
| 分类管理 | ✅ | ✅ | ✅ |
| Banner 管理 | ✅ | ✅ | ✅ |
| 内容管理 | ✅ | ✅ | ✅ |
| 询价管理 | ✅ | ✅ | ❌ |
| 评价管理 | ✅ | ✅ | ✅ |
| 客户管理 | ✅ | ✅ | ✅ |
| 场地设计 | ✅ | ✅ | ✅ |
| 媒体库 | ✅ | ✅ | ✅ |
| 多语言管理 | ✅ | ✅ | ❌ |
| 用户管理 | ✅ | ❌ | ❌ |
| 系统设置 | ✅ | ❌ | ❌ |
| 审计日志 | ✅ | 只读 | ❌ |

### 10.3 中间件保护

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  const token = request.cookies.get('next-auth.session-token');
  const path = request.nextUrl.pathname;

  // 未登录访问 /admin/* → 重定向到 /admin/login
  if (path.startsWith('/admin') && !path.includes('/admin/login') && !token) {
    return NextResponse.redirect(new URL('/admin/login', request.url));
  }

  // 已登录访问 /admin/login → 重定向到 /admin
  if (path === '/admin/login' && token) {
    return NextResponse.redirect(new URL('/admin', request.url));
  }
}
```

---

## 11. 缓存与性能策略

### 11.1 后台 vs 前台缓存策略差异

| 维度 | 前台 | 后台 |
|------|------|------|
| 读写比 | 读多写少（99:1） | 读写均衡 |
| 缓存侧重 | 减少 DB 查询 | 减少重复查询 |
| TTL | 5-30min | 1-5min（数据更需实时） |
| 缓存失效 | TTL 自动过期 | 后台写入时主动清除 |

### 11.2 后台缓存失效策略

```
后台任何写入操作后，主动清除相关缓存：

1. 产品更新 → 清除：
   - /api/products:* (前台产品列表缓存)
   - /api/products/:id (前台产品详情缓存)
   - /api/home (首页聚合缓存)
   - /api/admin/products:* (后台列表缓存)

2. 分类更新 → 清除：
   - /api/categories:* (前台分类缓存)
   - /api/home

3. Banner/内容/评价更新 → 清除：
   - /api/home

4. 询价状态变更 → 清除：
   - /api/admin/inquiries:* (后台询价缓存)
   - /api/admin/stats/* (统计缓存)
```

### 11.3 后台数据库查询优化

| 场景 | 优化 |
|------|------|
| 产品列表 | 只查主表+当前语言翻译，不 JOIN 所有语言 |
| 询价列表 | 分页查询走 idx_created 索引 |
| 统计查询 | 结果缓存 5min，不在每次请求时实时计算 |
| 批量操作 | 使用事务，单次连接多次操作 |
| 导出 Excel | 流式查询 + 流式写入，避免全量加载到内存 |
| 深分页 | 限制最大页数（如100页），超过则缩小查询范围 |

### 11.4 后台 API 响应要求

| 指标 | 目标 |
|------|------|
| 列表接口 | < 500ms |
| 详情接口 | < 300ms |
| 写入接口 | < 1s |
| 统计接口 | < 1s |
| 文件上传 | 视文件大小，≤10MB 图片 < 3s |

---

## 12. 工程规范

### 12.1 项目目录结构

```
src/
├── app/
│   ├── [locale]/              # 前台
│   ├── admin/                 # 后台
│   │   ├── layout.tsx         # 后台布局
│   │   ├── page.tsx           # 仪表盘
│   │   ├── login/page.tsx
│   │   ├── products/
│   │   │   ├── page.tsx       # 列表
│   │   │   ├── new/page.tsx   # 新增
│   │   │   └── [id]/page.tsx  # 编辑/详情
│   │   ├── categories/page.tsx
│   │   ├── banners/page.tsx
│   │   ├── content/page.tsx
│   │   ├── inquiries/
│   │   │   ├── page.tsx
│   │   │   └── [id]/page.tsx
│   │   ├── reviews/page.tsx
│   │   ├── clients/page.tsx
│   │   ├── venues/page.tsx
│   │   ├── media/page.tsx
│   │   ├── languages/page.tsx
│   │   ├── users/page.tsx
│   │   └── settings/page.tsx
│   └── api/
│       ├── admin/             # 后台 API
│       │   ├── products/
│       │   ├── categories/
│       │   ├── banners/
│       │   ├── content-blocks/
│       │   ├── inquiries/
│       │   ├── reviews/
│       │   ├── clients/
│       │   ├── venues/
│       │   ├── media/
│       │   ├── users/
│       │   ├── languages/
│       │   ├── stats/
│       │   └── settings/
│       └── ...                # 前台 API
├── components/
│   ├── ui/                    # shadcn/ui 基础组件
│   ├── admin/                 # 后台专用组件
│   │   ├── DataTable.tsx      # 通用数据表格
│   │   ├── ImageUpload.tsx    # 图片上传组件
│   │   ├── VideoUpload.tsx    # 视频上传组件
│   │   ├── RichEditor.tsx     # Tiptap 富文本编辑器
│   │   ├── SortableList.tsx   # 拖拽排序列表
│   │   ├── LanguageTabs.tsx   # 多语言 Tab 切换
│   │   ├── StatusBadge.tsx    # 状态标签
│   │   └── ConfirmDialog.tsx  # 确认弹窗
│   └── ...                    # 前台组件
├── lib/
│   ├── prisma.ts             # Prisma Client 单例
│   ├── redis.ts              # Redis Client 单例
│   ├── oss.ts                # 阿里云 OSS Client
│   ├── auth.ts               # NextAuth 配置
│   ├── cache.ts              # 缓存工具函数
│   └── audit.ts              # 审计日志工具
├── hooks/                     # 自定义 Hooks
├── types/                     # TypeScript 类型
├── messages/                  # i18n 文件
├── prisma/
│   ├── schema.prisma          # Prisma Schema
│   └── migrations/            # 数据库迁移
└── styles/
    └── globals.css
```

### 12.2 Prisma Schema 示例

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Product {
  id              String    @id @default(uuid())
  categoryId      String    @map("category_id")
  subCategoryId   String?   @map("sub_category_id")
  productKey      String    @unique @map("product_key")
  price           Decimal?
  coverImageUrl   String    @map("cover_image_url")
  baseLikeCount   Int       @default(0) @map("base_like_count")
  actualLikeCount Int       @default(0) @map("actual_like_count")
  tags            Json?
  sortOrder       Int       @default(0) @map("sort_order")
  status          Int       @default(1)
  isFeatured      Boolean   @default(false) @map("is_featured")
  createdAt       DateTime  @default(now()) @map("created_at")
  updatedAt       DateTime  @updatedAt @map("updated_at")
  deletedAt       DateTime? @map("deleted_at")

  category      Category     @relation("CategoryProducts", fields: [categoryId], references: [id])
  subCategory   Category?    @relation("SubCategoryProducts", fields: [subCategoryId], references: [id])
  translations  ProductTranslation[]
  images        ProductImage[]
  specs         ProductSpec[]
  likes         ProductLike[]
  inquiryProducts InquiryProduct[]

  @@index([categoryId])
  @@index([subCategoryId])
  @@index([status])
  @@index([sortOrder])
  @@map("products")
}

model ProductTranslation {
  id          String  @id @default(uuid())
  productId   String  @map("product_id")
  lang        String
  name        String
  summary     String?
  description String? @db.MediumText

  product Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([productId, lang])
  @@map("product_translations")
}

// ... 其他模型按数据库表结构对应
```

### 12.3 环境变量

```env
# 数据库
DATABASE_URL="mysql://user:password@localhost:3306/arcade_website"

# Redis
REDIS_URL="redis://localhost:6379"

# NextAuth
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="https://your-domain.com"

# 阿里云 OSS
OSS_ACCESS_KEY_ID="your-access-key"
OSS_ACCESS_KEY_SECRET="your-secret-key"
OSS_BUCKET="your-bucket"
OSS_REGION="oss-cn-shenzhen"
OSS_ENDPOINT="oss-cn-shenzhen.aliyuncs.com"
OSS_CDN_DOMAIN="cdn.your-domain.com"

# 站点
NEXT_PUBLIC_SITE_URL="https://your-domain.com"
```

### 12.4 初始数据

系统初始化时需插入以下种子数据：

```sql
-- 默认语言
INSERT INTO languages (id, code, name, native_name, is_default, sort_order) VALUES
(UUID(), 'zh', 'Chinese', '中文', 1, 1),
(UUID(), 'en', 'English', 'English', 0, 2);

-- 超级管理员（密码：admin123，bcrypt hash）
INSERT INTO users (id, email, name, password_hash, role) VALUES
(UUID(), 'admin@example.com', '超级管理员', '$2b$10$...', 'super_admin');

-- 询价选项
INSERT INTO inquiry_options (id, option_group, lang, value, sort_order) VALUES
(UUID(), 'budget', 'zh', '5万以下', 1),
(UUID(), 'budget', 'zh', '5-10万', 2),
(UUID(), 'budget', 'zh', '10-50万', 3),
(UUID(), 'budget', 'zh', '50万以上', 4),
(UUID(), 'budget', 'en', 'Under 50K', 1),
(UUID(), 'budget', 'en', '50K-100K', 2),
(UUID(), 'budget', 'en', '100K-500K', 3),
(UUID(), 'budget', 'en', 'Over 500K', 4),
(UUID(), 'purchase_time', 'zh', '1个月内', 1),
(UUID(), 'purchase_time', 'zh', '1-3个月', 2),
(UUID(), 'purchase_time', 'zh', '3-6个月', 3),
(UUID(), 'purchase_time', 'zh', '6个月以上', 4),
(UUID(), 'purchase_time', 'en', 'Within 1 month', 1),
(UUID(), 'purchase_time', 'en', '1-3 months', 2),
(UUID(), 'purchase_time', 'en', '3-6 months', 3),
(UUID(), 'purchase_time', 'en', 'Over 6 months', 4);

-- 内容区块
INSERT INTO content_blocks (id, block_key, sort_order) VALUES
(UUID(), 'why_choose_us', 1),
(UUID(), 'suitable_venues', 2),
(UUID(), 'purchase_process', 3),
(UUID(), 'footer', 4);
```

### 12.5 安全规范

| 项目 | 要求 |
|------|------|
| 密码存储 | bcrypt，salt rounds ≥ 10 |
| SQL 注入 | Prisma 参数化查询，禁止拼接 SQL |
| XSS | 富文本输出时服务端 sanitize |
| CSRF | NextAuth 内置 CSRF 保护 |
| 文件上传 | 验证文件类型、大小，不允许上传可执行文件 |
| JWT | httpOnly + Secure + SameSite=Strict |
| 日志脱敏 | 审计日志不记录密码等敏感信息 |
| 接口限流 | 后台 API 限流 100次/分钟/IP |

---

> **文档说明**：本文档为后台管理系统开发需求文档 v1.0，与《官网前台开发需求文档 v2.0》配套使用。同一 Next.js 项目，前台 SSR + 后台 CSR，共享数据库和 ORM。后台所有写入操作需清除前台对应缓存，确保前台数据实时更新。
