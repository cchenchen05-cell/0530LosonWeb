---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 7625865517047251251-data_volume/files/所有对话/主对话/官网前台开发需求文档/官网前台SPEC_v1.0.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 2570010760323498#1780124284074
    ReservedCode2: ""
---
# Loson科技官网前台 产品规格文档 (SPEC)

> **项目**: 电玩设备源头工厂制造商官网（B2B+B2C展示型）  
> **版本**: v1.1  
> **状态**: 已更新  
> **日期**: 2025年

---

## 目录

- [第1章 项目概述与愿景](#第1章-项目概述与愿景)
- [第2章 设计语言](#第2章-设计语言)
- [第3章 布局与结构](#第3章-布局与结构)
- [第4章 功能模块详解](#第4章-功能模块详解)
- [第5章 组件清单](#第5章-组件清单)
- [第6章 技术选型](#第6章-技术选型)
- [第7章 文件结构](#第7章-文件结构)
- [第8章 Mock数据说明](#第8章-mock数据说明)
- [第9章 验收标准](#第9章-验收标准)
- [附录](#附录)

---

## 第1章 项目概述与愿景

### 1.1 项目背景

Loson科技是电玩娱乐设备源头工厂制造商，拥有15年以上行业经验，产品覆盖拳击机、娃娃机、赛车机、VR设备、飞镖机、音乐机、体育机等7大品类。本项目旨在打造一个兼具品牌展示与营销转化能力的官网，同时服务B2B（游乐场馆、商场、娱乐中心）和B2C（个人玩家）客户群体。

**核心定位**：展示型网站，无在线交易系统，通过询价表单实现营销转化。

### 1.2 产品愿景

| 维度 | 目标 |
|------|------|
| **品牌形象** | 打造科技感、专业感、国际化的企业形象，传达"源头工厂"的可信度 |
| **产品展示** | 结构化展示7大产品线，支持多维度筛选，高效触达目标产品 |
| **营销转化** | 提供便捷的询价渠道，降低B2B沟通成本，缩短决策周期 |
| **用户洞察** | 无登录情况下追踪用户行为，后台可视化展示，支撑运营决策 |
| **SEO获客** | 通过SSR和结构化数据，提升搜索引擎排名，获取有机流量 |

### 1.3 核心用户画像

| 用户类型 | 典型角色 | 核心诉求 | 关键路径 |
|---------|---------|---------|---------|
| B2B采购商 | 游乐场运营者、商场招商经理 | 批量采购、定制方案、售后保障 | 首页→产品分类→产品详情→询价 |
| B2C消费者 | 个人玩家、小型店主 | 产品信息、价格参考、购买渠道 | 首页→热门产品→详情页→咨询 |
| 海外客户 | 外贸采购商 | 多语言支持、出口资质、国际物流 | 首页(EN)→Products→Inquiry |

### 1.4 用户身份追踪方案（重点）

本网站不要求用户登录，但需要在匿名情况下尽可能准确地追踪用户身份与行为，为运营决策提供数据支撑。

#### 1.4.1 身份标识策略

| 层级 | 标识方式 | 持久性 | 说明 |
|------|---------|--------|------|
| L1-核心 | `visitor_id` (UUID v4) | 长期（365天） | 首次访问时生成，存入 localStorage + Cookie + IndexedDB 三重冗余 |
| L2-辅助 | `session_id` (UUID v4) | 会话级 | 每次新会话生成，30分钟无活动过期 |
| L3-指纹 | 浏览器指纹 (Canvas + WebGL + Audio) | 中期 | 作为兜底方案，当Cookie/Storage均不可用时辅助识别 |
| L4-合并 | 设备特征组合 | 长期 | UserAgent + 屏幕分辨率 + 时区 + 语言组合，用于跨浏览器指纹关联 |

**生成逻辑**：

```
首次访问：
1. 检查 localStorage 中的 visitor_id → 有则使用
2. 无则检查 Cookie 中的 visitor_id → 有则同步回 localStorage
3. 均无则生成新 UUID v4，同时写入 localStorage + Cookie(maxAge=365d) + IndexedDB
4. 生成 session_id，记录首次访问时间戳

回访识别：
1. localStorage 有 visitor_id → 直接使用（最常见场景）
2. 无痕浏览器：Cookie 不可用 → 降级到浏览器指纹匹配
3. 跨设备：无法关联 → 视为新用户（可接受）
```

#### 1.4.2 行为数据采集范围

| 事件类型 | 采集字段 | 触发时机 |
|---------|---------|---------|
| `page_view` | visitor_id, session_id, page_url, referrer, duration | 页面加载完成 + 离开页面时计算停留时长 |
| `product_view` | visitor_id, product_id, product_category, view_duration | 产品详情页进入 + 离开 |
| `category_browse` | visitor_id, category_id, filter_params | 产品列表页筛选操作 |
| `search` | visitor_id, keyword, result_count | 搜索提交 |
| `inquiry_submit` | visitor_id, product_id, form_data(脱敏) | 询价表单提交 |
| `favorite_add` | visitor_id, product_id | 添加收藏 |
| `scroll_depth` | visitor_id, page_url, depth_percent | 滚动深度达到 25%/50%/75%/100% |
| `cta_click` | visitor_id, cta_type, cta_location | CTA按钮点击 |
| `referrer_track` | visitor_id, session_id, referrer_url, source_type, source_detail, utm_source, utm_medium, utm_campaign | 首次进入站点时采集来源信息 |
| `geo_info` | visitor_id, country, region, city, timezone | 首次进入站点或IP变化时采集地理位置 |
| `page_exit` | visitor_id, session_id, page_url, duration, scroll_max | 离开页面时上报完整停留数据 |

#### 1.4.3 数据传输策略（平衡性能与DB压力）

**核心原则**：批量上报 + 防丢兜底 + 智能节流

| 策略 | 具体方案 | 说明 |
|------|---------|------|
| **定时批量上报** | 每30秒将缓冲区中的行为事件打包发送一次 | 减少请求次数，降低DB写入压力 |
| **事件量阈值** | 缓冲区累积≥10条事件时立即上报 | 高活跃用户不会丢失数据 |
| **关键事件即时上报** | `inquiry_submit` / `favorite_add` 不走缓冲，立即发送 | 转化类事件优先级最高 |
| **页面离开上报** | 监听 `beforeunload` / `visibilitychange`，使用 `navigator.sendBeacon()` 发送 | 确保关闭浏览器/切换后台时数据不丢失 |
| **离线缓存** | 发送失败时将数据存入 IndexedDB，下次上线后重试 | 网络异常场景的兜底 |
| **去重机制** | 每条事件携带 `event_id` (UUID)，后端幂等校验 | 防止重复写入 |

**数据流时序图**：

```
用户操作 → 事件入缓冲区(Buffer) → 定时器/阈值触发 → POST /api/track (批量)
                                              ↓ 失败
                                         IndexedDB暂存
                                              ↓ 下次上线
                                         重试发送
                                              
页面关闭 → beforeunload → navigator.sendBeacon() → POST /api/track
                                              (不阻塞页面卸载)
```

**上报频率对照表**：

| 场景 | 上报间隔 | 批量大小 | DB写入QPS估算 |
|------|---------|---------|-------------|
| 低活跃（阅读页面） | 30s | 1-3条 | ~0.1 QPS/用户 |
| 中活跃（浏览产品） | 30s | 5-10条 | ~0.3 QPS/用户 |
| 高活跃（快速筛选） | 15s(阈值触发) | 10+条 | ~0.7 QPS/用户 |
| 关键事件 | 即时 | 1条 | 按需 |

**1000并发用户场景下DB写入峰值**：~300 QPS，完全在MySQL可承受范围内。

**来源识别策略**：

| 来源类型 | 识别规则 | 示例 |
|---------|---------|------|
| direct | referrer 为空或为自身域名 | 直接输入URL、书签访问 |
| organic | referrer 包含搜索引擎域名 | Google、Bing、百度、Yandex、DuckDuckGo |
| social | referrer 包含社交媒体域名 | Facebook、Twitter、LinkedIn、YouTube、Instagram、TikTok |
| referral | referrer 为其他外部域名 | 行业论坛、合作伙伴网站 |
| paid | URL 含 utm_medium=cpc/ppc/paid | Google Ads、Facebook Ads |
| email | URL 含 utm_medium=email 或 utm_source=newsletter | 邮件营销活动 |

**UTM参数采集**：URL中的 utm_source、utm_medium、utm_campaign、utm_term、utm_content 五个参数，在首次进入站点时一次性采集并写入当前会话，后续页面切换不再重复采集。

**地理位置采集**：
- 不在前端采集GPS（隐私敏感）
- 通过后台 API 自动根据请求IP解析（使用 MaxMind GeoLite2 或 ip2region）
- 前端只采集 timezone 作为辅助地理信息
- IP地址不在数据库中存储，只保留解析后的国家/省/城市

#### 1.4.4 防丢数据策略

| 风险场景 | 防丢措施 | 数据可靠性 |
|---------|---------|-----------|
| 用户关闭浏览器 | `beforeunload` + `sendBeacon()` | ≥99% |
| 用户切换到后台 | `visibilitychange` 事件触发缓冲上报 | ≥99% |
| 网络断开 | IndexedDB暂存 + 上线重试（最多3次，指数退避） | ≥95% |
| 服务器异常 | 客户端重试 + 本地保留7天数据 | ≥90% |
| 无痕模式 | 浏览器指纹降级识别 + session级内存缓冲 + 离开时sendBeacon | ≥80% |
| 多标签页 | 使用 BroadcastChannel 同步 visitor_id，避免重复生成 | ≥99% |

#### 1.4.5 后台可视化展示

| 看板模块 | 数据指标 | 展示形式 |
|---------|---------|---------|
| 实时访客 | 在线人数、当前页面分布 | 实时数字 + 地图热力 |
| 流量概览 | UV/PV、新老访客比、平均停留时长 | 折线图 + 趋势对比 |
| 产品热度 | 产品浏览TOP10、分类访问分布 | 柱状图 + 饼图 |
| 转化漏斗 | 访问→产品浏览→询价提交 | 漏斗图 |
| 搜索热词 | TOP20搜索关键词 | 词云 + 排行榜 |
| 地域分布 | 访客地域分布 | 地图热力图 |
| 用户行为路径 | 页面间流转路径（桑基图）、入口/出口页面、询价前路径 | 桑基图 + 路径列表 |
| 实时访客 | 当前在线人数、浏览页面、来源、地域 | 实时数字 + 滚动列表 |
| 来源渠道分析 | 各来源类型的UV/PV/询价数/转化率 | 柱状图 + 表格 |
| UTM活动分析 | 按UTM参数分组的流量和转化数据 | 表格 + 趋势对比 |

> 参考组件：Recharts 图表库

### 1.5 项目约束

| 约束类型 | 具体要求 |
|---------|---------|
| 浏览器支持 | Chrome 90+, Firefox 90+, Safari 14+, Edge 90+ |
| 响应式 | 必须支持移动端（<768px）、平板（768-1023px）、桌面（≥1024px） |
| 无障碍 | 图片必须 alt 属性，表单必须有 label，焦点管理符合 WCAG 2.1 AA |
| SEO | Lighthouse SEO评分 ≥ 90 |
| 性能 | FCP < 1.5s, LCP < 2.5s, CLS < 0.1 |
| 多语言 | 当前中文，架构预留英文扩展能力（next-intl） |
| 隐私合规 | 不存储用户IP原始值，只存GeoIP解析后的国家/城市；Cookie/LocalStorage需用户同意（GDPR）；浏览数据6个月后自动归档 |

---

## 第2章 设计语言

### 2.1 设计风格定义

**核心风格**：拟物化 + 科技感 + 深色主题 + 霓虹发光

与电商管理后台的"Professional Dashboard"（白底、表格、数据驱动）风格完全不同，本网站追求视觉冲击力和沉浸感，通过深色背景、霓虹发光、3D翻转等效果，传达电玩行业的激情与未来感。

| 设计原则 | 实现方式 |
|---------|---------|
| 层次感 | 阴影、渐变、背景色区分信息层级，发光效果突出重点 |
| 呼吸感 | 充足留白，Section间距96-112px |
| 微交互 | 3D翻转、发光追踪、流光边框、confetti等丰富动效 |
| 一致性 | 全站统一深色主题、统一组件状态、统一动效曲线 |

### 2.2 色彩系统

#### 2.2.1 主色调

| 色彩名称 | 色值 | 用途 | 情感表达 |
|---------|------|------|---------|
| **科技蓝** | `#3B82F6` | Logo、主CTA按钮、发光效果 | 专业、科技、信任 |
| **活力紫** | `#8B5CF6` | 渐变端点、装饰元素、分类标签 | 创新、活力、未来 |

#### 2.2.2 强调色

| 色彩名称 | 色值 | 用途 |
|---------|------|------|
| **霓虹绿** | `#22C55E` | 热门标签、新品标识、成功状态 |
| **热力橙** | `#F97316` | 促销标识、次要CTA |
| **警告红** | `#EF4444` | 错误状态、必填标记 |

#### 2.2.3 中性色（深色主题）

| 色彩名称 | 色值 | 用途 |
|---------|------|------|
| **主背景** | `#0F172A` | 页面主体背景 |
| **卡片背景** | `#1E293B` | 内容卡片、模块背景 |
| **悬浮背景** | `#334155` | 悬停状态、高亮区域 |
| **分割线** | `rgba(255,255,255,0.06)` | 卡片边框、分割线 |
| **主文字** | `#F8FAFC` | 标题、重要文字 |
| **次要文字** | `#94A3B8` | 正文、描述文字 |
| **辅助文字** | `#64748B` | 提示文字、时间戳 |

#### 2.2.4 渐变色

| 渐变名称 | CSS值 | 用途 |
|---------|-------|------|
| 科技渐变 | `linear-gradient(135deg, #3B82F6 0%, #8B5CF6 100%)` | 主按钮、Hero区装饰 |
| 霓虹渐变 | `linear-gradient(135deg, #22C55E 0%, #10B981 100%)` | 热门标签 |
| 活力渐变 | `linear-gradient(135deg, #F97316 0%, #EF4444 100%)` | 促销标签 |
| 流光边框 | `conic-gradient(from var(--angle), #3B82F6, #8B5CF6, #EC4899, #3B82F6)` | 搜索框旋转流光 |

#### 2.2.5 色彩应用规范

- **60-30-10 法则**：60% 深色背景（#0F172A + #1E293B）、30% 主色调（#3B82F6 + #8B5CF6）、10% 强调色（#F97316 + #22C55E）
- **文字对比度**：主文字与背景 ≥ 4.5:1，次要文字与背景 ≥ 3:1
- **发光效果**：统一使用 `box-shadow: 0 0 20px rgba(59, 130, 246, 0.3)` 模式

### 2.3 字体系统

| 用途 | 字体族 | 字号（桌面） | 字号（移动） | 字重 |
|------|--------|------------|------------|------|
| 大标题 (Hero) | Inter / system-ui | 56px | 32px | 800 |
| 页面标题 (H1) | Inter / system-ui | 40px | 28px | 700 |
| 模块标题 (H2) | Inter / system-ui | 32px | 24px | 700 |
| 卡片标题 (H3) | Inter / system-ui | 20px | 18px | 600 |
| 正文 | Inter / system-ui | 16px | 14px | 400 |
| 辅助文字 | Inter / system-ui | 14px | 12px | 400 |
| 标签/Badge | Inter / system-ui | 12px | 11px | 500 |

> 字体加载策略：使用 `next/font` 自动优化，`display: swap`，仅加载 400/600/700/800 字重

### 2.4 间距系统

采用 Tailwind CSS 间距规范（4px基数）：

| 用途 | 间距值 | Token名 |
|------|--------|---------|
| 组件内间距 | 8px / 12px / 16px | p-2 / p-3 / p-4 |
| 卡片内间距 | 20px / 24px | p-5 / p-6 |
| Section垂直间距 | 80px / 96px / 112px | py-20 / py-24 / py-28 |
| 页面水平内边距 | 16px(sm) / 32px(md) / 48px(lg) / 64px(xl) | px-4 / px-8 / px-12 / px-16 |
| 卡片间距 | 16px / 24px / 32px | gap-4 / gap-6 / gap-8 |

### 2.5 动效规范

| 动效类型 | 持续时间 | 缓动函数 | 应用场景 |
|---------|---------|---------|---------|
| 快速过渡 | 200ms | ease-out | 按钮、小元素hover |
| 标准过渡 | 300ms | ease-out | 卡片、菜单、下拉 |
| 慢速过渡 | 500ms | ease-out | 页面切换、大图变化 |
| 入场动画 | 600ms | ease-out | 元素淡入上浮 |
| 交错入场 | stagger 100ms | ease-out | 卡片组依次入场 |
| 弹性动画 | spring(400,17) | spring | 按钮点击、模态框 |
| 3D翻转 | 500ms | ease-in-out | 导航菜单 glow-menu |
| 流光旋转 | 3s | linear infinite | 搜索框 conic-gradient |

> 参考组件：Framer Motion 动画库

### 2.6 视觉资产

| 资产类型 | 规格 | 说明 |
|---------|------|------|
| Logo | SVG + PNG@2x | 亮色/暗色两版 |
| 产品图片 | 1200×900px, WebP | 主图 + 缩略图自动生成 |
| 产品分类图标 | 64×64px, SVG | 7大品类各一个 |
| Hero背景视频 | MP4, 1080p, ≤10MB | 自动播放、静音、循环 |
| OG图片 | 1200×630px | 社交分享默认图 |
| Favicon | ICO + SVG | 多尺寸 |

---

## 第3章 布局与结构

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────┐
│                   全局固定元素层                       │
│  ┌──────┐                              ┌──────┐      │
│  │ 收藏  │  (左侧固定)                  │ 搜索  │      │
│  │ 图标  │                              │ 图标  │      │
│  └──────┘                              └──────┘      │
├──────────────────────────────────────────────────────┤
│                   导航栏 (固定顶部)                    │
│  Logo | 首页 | 产品中心▼ | 联系我们 | 场地设计 | 关于我们▼ | 🔍 | ❤️ │
├──────────────────────────────────────────────────────┤
│                                                      │
│                   页面内容区                          │
│                   (随路由切换)                        │
│                                                      │
├──────────────────────────────────────────────────────┤
│                   页脚                                │
│  公司信息 | 产品分类 | 快速链接 | 联系方式              │
└──────────────────────────────────────────────────────┘
```

### 3.2 响应式断点

| 断点名称 | 宽度范围 | 栅格列数 | 间距 | 导航形式 |
|---------|---------|---------|------|---------|
| xs | < 640px | 1列 | 16px | 汉堡菜单 |
| sm | 640px - 767px | 2列 | 16px | 汉堡菜单 |
| md | 768px - 1023px | 2-3列 | 24px | Logo + 简化菜单 |
| lg | 1024px - 1279px | 3-4列 | 24px | 完整水平导航 |
| xl | ≥ 1280px | 4-5列 | 32px | 完整水平导航 |

### 3.3 页面结构模式

所有页面遵循统一的结构模式：

```
<Layout>
  <Navbar />              <!-- 固定顶部导航 -->
  <FloatingActions />     <!-- 左侧固定：收藏+搜索 -->
  <main>
    <PageHeader />        <!-- 页面横幅/面包屑 -->
    <PageContent />       <!-- 页面主体内容 -->
  </main>
  <Footer />              <!-- 页脚 -->
</Layout>
```

### 3.4 导航栏结构

#### 3.4.1 菜单项

| 菜单项 | 下拉类型 | 下拉内容 |
|--------|---------|---------|
| 首页 | 无 | 直接跳转 |
| 产品中心 | **多级下拉** | 产品 → 分类 → 子类 → 图片 |
| 联系我们 | 无 | 直接跳转 |
| 场地设计 | 无 | 直接跳转 |
| 关于我们 | **单级下拉** | 企业简介、发展历程、团队、资质 |
| 搜索图标 🔍 | 弹出搜索框 | Google AI Studio风格流光搜索框 |
| 收藏图标 ❤️ | 弹出收藏列表 | 侧边抽屉，展示已收藏产品 |

#### 3.4.2 导航栏交互规则

| 交互元素 | 行为描述 |
|---------|---------|
| 菜单项 hover | **glow-menu 交互**：3D翻转 + 发光效果。参考组件：glow-menu.tsx |
| 产品中心 hover | 展开多级下拉菜单，子菜单悬浮于内容区上方 |
| 关于我们 hover | 展开单级下拉菜单 |
| 搜索图标点击 | 打开全屏搜索覆盖层，搜索框带 conic-gradient 旋转流光边框 |
| 收藏图标点击 | 右侧滑出收藏抽屉 |
| 滚动后导航栏 | 背景变为 `rgba(15,23,42,0.95)`，添加底部阴影和玻璃态效果 |

**重要**：搜索图标和收藏图标不参与 glow-menu 的3D翻转交互，它们保持普通 hover 效果。

#### 3.4.3 导航栏双主题

| 主题 | 背景 | 文字色 | 触发条件 |
|------|------|--------|---------|
| 暗色系 | `rgba(15,23,42,0.8)` + backdrop-blur | `#F8FAFC` | 默认 / 滚动后 |
| 白色系 | `rgba(255,255,255,0.9)` + backdrop-blur | `#0F172A` | 可选（预留切换能力） |

### 3.5 全局浮动元素

| 元素 | 位置 | 功能 | 交互 |
|------|------|------|------|
| 收藏图标 | 左侧窗口固定，垂直居中偏下 | 点击跳转表单页或打开收藏列表 | hover发光，点击弹窗/跳转 |
| 搜索图标 | 左侧窗口固定，收藏图标上方 | 点击打开搜索覆盖层 | hover发光，点击弹出搜索 |

---

## 第4章 功能模块详解

### 4.1 首页

#### 4.1.1 页面布局

首页由10个垂直排列的 Section 组成，从上到下依次为：

```
┌───────────────────────────────────┐
│ 1. Hero 视频背景区                 │  全屏
├───────────────────────────────────┤
│ 2. 热门产品分类                    │  不规则5列网格
├───────────────────────────────────┤
│ 3. 热门产品                        │  4列产品卡片网格
├───────────────────────────────────┤
│ 4. 为什么选择我们                   │  ExpandingCards 展开卡片
├───────────────────────────────────┤
│ 5. 客户评论                        │  左右布局
├───────────────────────────────────┤
│ 6. 场地设计展示                    │  图文网格
├───────────────────────────────────┤
│ 7. 产品适合什么地方                 │  场景图集
├───────────────────────────────────┤
│ 8. 采购流程                        │  步骤流程图
├───────────────────────────────────┤
│ 9. 表单                            │  联系/询价表单
├───────────────────────────────────┤
│ 10. 页脚                          │  4列信息布局
└───────────────────────────────────┘
```

#### 4.1.2 Section 1: Hero 英雄区

**页面布局**：全屏视频背景 + 居中品牌文案 + CTA按钮

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 视频背景自动播放 | P0 | 静音、循环、自动播放 |
| 品牌文案展示 | P0 | 主标题 + 副标题 + 数据指标 |
| CTA按钮组 | P0 | "查看产品" + "获取报价" |
| 滚动指示器 | P1 | 底部向下箭头浮动动画 |
| 视差滚动 | P2 | 内容区滚动时Hero区视差效果 |

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 主标题 | string | 是 | "Loson科技" |
| 副标题 | string | 是 | "15+年行业经验 · 源头工厂 · 全球服务" |
| 视频URL | file | 是 | Hero背景视频 |
| CTA按钮1文案 | string | 是 | "查看产品" → /products |
| CTA按钮2文案 | string | 是 | "获取报价" → /contact |
| 数据指标 | array | 否 | [{value: "15+", label: "年行业经验"}, {value: "1000+", label: "成功案例"}] |

**交互规则**：
- 视频加载中显示封面图 + 渐变遮罩
- 文字淡入动画（stagger 100ms）
- CTA按钮 hover：放大1.02x + 阴影扩散 + 渐变位移
- 点击"查看产品"：平滑滚动到热门产品分类区域
- 点击"获取报价"：跳转联系我们页面
- 滚动指示器：上下浮动动画（1.5s infinite）

#### 4.1.3 Section 2: 热门产品分类

**页面布局**：不规则5列网格布局

**布局规格**：`grid-template-columns: 300fr 600fr 300fr 280fr 280fr`

| 分类 | 布局位置 | 展示方式 |
|------|---------|---------|
| 拳击机 | 第1列 | 图片 + 标题 + 说明（默认隐藏，hover显示） |
| 娃娃机 | 第2列（大） | 图片 + 标题 + 说明（默认隐藏，hover显示） |
| 赛车机 | 第3列 | 图片 + 标题 + 说明（默认隐藏，hover显示） |
| VR设备 | 第4列 | 图片 + 标题 + 说明（默认隐藏，hover显示） |
| 飞镖机 | 第5列(上) | 图片 + 标题 + 说明（默认隐藏，hover显示） |
| 音乐机 | 第5列(下) | 图片 + 标题 + 说明（默认隐藏，hover显示） |
| 体育机 | 跨列 | 图片 + 标题 + 说明（默认隐藏，hover显示） |

> 参考组件：gourmet-grid HTML 不规则网格

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 7大分类展示 | P0 | 图片 + 标题 + 简短说明 |
| Hover展开说明 | P0 | 默认隐藏文字说明，hover时从底部滑入显示 |
| 点击跳转分类页 | P0 | 点击分类卡片跳转对应产品分类列表 |

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 分类名称 | string | 是 | 如"拳击机" |
| 分类图片 | file | 是 | 高清产品图片 |
| 分类说明 | string | 是 | 简短描述，如"专业级拳击竞技设备" |
| 跳转链接 | string | 是 | 如"/products/punching-machines" |
| 网格位置 | json | 是 | 控制不规则布局中的位置和跨度 |

**交互规则**：
- 默认状态：仅显示图片 + 标题，说明文字隐藏
- Hover：说明文字从底部滑入（translateY 100% → 0，300ms），图片轻微缩放（scale 1.05）
- 点击：跳转对应分类产品列表页
- 移动端：说明文字始终可见（无hover状态），改为2列网格

#### 4.1.4 Section 3: 热门产品

**页面布局**：4列产品卡片网格 + "查看更多"按钮

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 热门产品展示 | P0 | 最多展示12个产品，SpotlightCard发光追踪效果 |
| "查看更多"按钮 | P0 | 跳转产品中心页 |
| 产品收藏 | P1 | 卡片右上角收藏图标 |

> 参考组件：SpotlightCard 发光追踪效果

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 产品ID | uuid | 是 | 唯一标识 |
| 产品名称 | string | 是 | 如"超级娃娃机X1" |
| 产品缩略图 | file | 是 | 产品主图 |
| 产品分类 | string | 是 | 所属分类名称 |
| 标签 | array | 否 | [热门/新品/推荐] |
| 简短描述 | string | 否 | 1-2句话描述 |

**交互规则**：
- 卡片 hover：SpotlightCard效果 — 鼠标位置产生光晕追踪，卡片边框发光
- 点击卡片：跳转产品详情页
- 收藏图标点击：添加到收藏列表（localStorage + 后台同步）
- "查看更多"：跳转 `/products`
- 产品卡片入场：滚动进入视口时依次淡入上浮（stagger 80ms）

#### 4.1.5 Section 4: 为什么选择我们

**页面布局**：4个 ExpandingCards 展开卡片，水平排列

> 参考组件：ExpandingCards 展开卡片

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 4大优势展示 | P0 | 每个卡片：图片 + 标题 + 详情 |
| 展开交互 | P0 | 点击/-hover时卡片展开，其余卡片收缩 |
| 动画过渡 | P1 | 展开/收缩平滑动画（500ms） |

**4大优势点**：

| 序号 | 标题 | 说明 |
|------|------|------|
| 1 | 品质保证 | ISO认证，3年质保，源头工厂直供 |
| 2 | 快速交付 | 7天内发货，全球物流网络 |
| 3 | 专业售后 | 24小时技术支持，远程诊断 |
| 4 | 全球服务 | 覆盖50+国家，本地化服务团队 |

**交互规则**：
- 默认：4卡片等宽，显示图片 + 标题
- Hover/点击某卡片：该卡片展开至宽度占比50%，其余收缩至~16.7%
- 展开卡片显示：完整图片 + 标题 + 详细说明
- 收缩卡片仅显示：图片 + 标题
- 移动端：改为垂直手风琴（Accordion）模式

#### 4.1.6 Section 5: 客户评论

**页面布局**：左右分栏布局

```
┌─────────────────────────┬───────────────────┐
│  左侧                   │  右侧              │
│  ┌───────────────────┐  │  ┌───────────────┐ │
│  │ 客户头像+姓名+公司  │  │  │               │ │
│  │ 评分（星级）        │  │  │  视频播放器    │ │
│  │ 评论内容           │  │  │               │ │
│  │ 评论产品图片       │  │  │               │ │
│  └───────────────────┘  │  └───────────────┘ │
│                         │                    │
│  ┌─────────────────────┐│                    │
│  │ 底部头像轮播         ││                    │
│  │ + 七彩描边动画       ││                    │
│  └─────────────────────┘│                    │
└─────────────────────────┴───────────────────┘
```

> 参考组件：客户评论完整HTML布局

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 客户评论展示 | P0 | 姓名、公司、评分、评论文本、产品图 |
| 视频播放器 | P0 | 右侧播放客户案例视频 |
| 头像轮播 | P1 | 底部头像横向滚动，七彩描边动画标识当前选中 |
| 评论切换 | P1 | 点击底部头像切换左侧评论内容 |

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 客户姓名 | string | 是 | 评论者姓名 |
| 客户公司 | string | 否 | 评论者所属公司 |
| 客户头像 | file | 是 | 头像图片 |
| 评分 | number | 是 | 1-5星级 |
| 评论内容 | text | 是 | 评论文本 |
| 产品图片 | file | 否 | 评论关联的产品图片 |
| 案例视频 | file | 否 | 右侧视频播放器的视频源 |

**交互规则**：
- 头像轮播：自动播放（5s/张），hover暂停
- 当前选中头像：七彩描边动画（`border-image: conic-gradient` 旋转）
- 点击头像：切换左侧评论内容 + 右侧视频
- 视频播放器：点击播放/暂停，进度条拖拽

#### 4.1.7 Section 6: 场地设计展示

**页面布局**：图文网格，每项包含图片 + 标题 + 描述

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 场地案例展示 | P0 | 展示3-6个场地设计案例 |
| 点击查看详情 | P1 | 跳转场地设计页面 |

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 标题 | string | 是 | 如"大型商场电玩城" |
| 封面图 | file | 是 | 场地实景图 |
| 描述 | string | 是 | 简短描述 |
| 链接 | string | 否 | 跳转详情页 |

#### 4.1.8 Section 7: 产品适合什么地方

**页面布局**：场景图集/网格展示，每个场景配图标 + 名称

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 适用场景展示 | P0 | 如：商场、游乐场、电影院、餐厅等 |
| hover交互 | P1 | hover时显示场景详情 |

**字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 场景名称 | string | 是 | 如"购物中心" |
| 场景图片 | file | 是 | 场景配图 |
| 场景描述 | string | 否 | 适用产品和建议 |

#### 4.1.9 Section 8: 采购流程

**页面布局**：水平步骤流程图

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 采购步骤展示 | P0 | 4-5步骤流程 |
| 步骤动画 | P1 | 滚动到视口时步骤依次入场 |

**采购步骤**：

| 步骤 | 标题 | 说明 |
|------|------|------|
| 1 | 咨询需求 | 告诉我们您的场地和预算 |
| 2 | 方案设计 | 专业团队为您定制方案 |
| 3 | 确认订单 | 确认产品配置和价格 |
| 4 | 生产制造 | 源头工厂按标准生产 |
| 5 | 交付安装 | 全球配送 + 专业安装 |

#### 4.1.10 Section 9: 表单

**页面布局**：居中表单卡片，渐变背景

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 联系表单 | P0 | 姓名、电话、邮箱、公司、留言 |
| 表单验证 | P0 | React Hook Form + Zod 实时验证 |
| 提交动画 | P1 | 提交成功后 confetti 彩纸飞舞 + 弹跳动画 |

> 参考组件：表单提交成功动画（confetti + 弹跳）、React Hook Form + Zod

**字段说明**：

| 字段 | 类型 | 必填 | 验证规则 | 说明 |
|------|------|------|---------|------|
| 姓名 | string | 是 | 2-50字符 | 中文或英文名 |
| 电话 | string | 是 | 手机号正则 | 中国大陆手机号 |
| 邮箱 | string | 否 | 邮箱正则 | 用于回复 |
| 公司 | string | 否 | 2-200字符 | 公司名称 |
| 感兴趣的产品 | select | 否 | 7大分类 | 产品类型选择 |
| 留言 | textarea | 是 | 10-2000字符 | 咨询内容 |

**交互规则**：
- 输入框聚焦：边框发光效果（`box-shadow: 0 0 0 3px rgba(59,130,246,0.2)`）
- 实时验证：失焦后验证，错误信息红色显示在输入框下方
- 提交中：按钮 loading 状态（spinner + "提交中..."）
- 提交成功：confetti 彩纸飞舞动画 + 弹跳弹窗"提交成功！我们将尽快联系您"
- 提交失败：Toast 提示错误信息
- 同时记录 `inquiry_submit` 事件到用户行为追踪系统

> 参考组件：Alert/Toast 通知组件

#### 4.1.11 Section 10: 页脚

**页面布局**：4列信息布局 + 底部版权栏

| 列 | 内容 |
|----|------|
| 第1列 | 公司Logo + 简介文字 |
| 第2列 | 产品分类链接列表 |
| 第3列 | 快速链接（关于我们、联系我们等） |
| 第4列 | 联系方式（电话、邮箱、地址）+ 社交媒体图标 |

---

### 4.2 产品中心页

#### 4.2.1 页面布局

```
┌───────────────────────────────────────────────────┐
│ 页面横幅 + 面包屑                                  │
├───────────────────────────────────────────────────┤
│ 搜索框（流光边框）                                 │
├──────────┬────────────────────────────────────────┤
│ 筛选侧栏  │  产品网格（SpotlightCard）              │
│ ·分类筛选  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐         │
│ ·价格筛选  │  │卡片│ │卡片│ │卡片│ │卡片│         │
│ ·标签筛选  │  └────┘ └────┘ └────┘ └────┘         │
│           │  ┌────┐ ┌────┐ ┌────┐ ┌────┐         │
│           │  │卡片│ │卡片│ │卡片│ │卡片│         │
│           │  └────┘ └────┘ └────┘ └────┘         │
│           │                                        │
│           │  分页 / 加载更多                         │
├──────────┴────────────────────────────────────────┤
```

#### 4.2.2 多级下拉导航

产品中心的导航采用多级下拉结构：

```
产品中心
├── 拳击机系列
│   ├── 竞技型拳击机
│   ├── 娱乐型拳击机
│   └── 儿童型拳击机
│       └── [产品图片预览]
├── 娃娃机系列
│   ├── 商用娃娃机
│   └── 礼品机
│       └── [产品图片预览]
├── 赛车机系列
│   └── ...
├── VR设备系列
│   └── ...
├── 飞镖机系列
│   └── ...
├── 音乐机系列
│   └── ...
└── 体育机系列
    └── ...
```

#### 4.2.3 搜索框

**交互**：Google AI Studio 风格搜索框

> 参考组件：Google AI Studio 搜索框流光效果

| 特性 | 说明 |
|------|------|
| 流光边框 | `conic-gradient` 旋转流光，CSS `@property --angle` 动画驱动 |
| 居中放大 | 页面初始状态搜索框居中展示，输入后缩小到顶部 |
| 搜索建议 | 输入时实时显示搜索建议下拉列表 |
| 语音搜索 | P2优先级，预留麦克风图标 |

**交互规则**：
- 初始状态：搜索框居中页面，大尺寸，流光边框旋转
- 聚焦：流光速度加快，边框亮度增强
- 输入：实时显示搜索建议（防抖300ms）
- 提交：跳转搜索结果页，或原地筛选产品列表

#### 4.2.4 筛选功能

| 筛选维度 | 控件类型 | 说明 |
|---------|---------|------|
| 分类筛选 | 多级树形 | 支持多级分类展开/折叠 |
| 价格筛选 | Range Slider | 价格区间拖拽选择 |
| 标签筛选 | 标签组 | 多选标签，如"热门""新品""推荐" |
| 排序 | 下拉选择 | 最新、最热、价格升序、价格降序 |

**交互规则**：
- 筛选参数同步到URL（`?category=xxx&price_min=1000&sort=-date_created`）
- URL可直接恢复筛选状态，支持分享
- 移动端：筛选栏折叠为底部抽屉，点击"筛选"按钮展开
- 空结果：显示空状态插画 + "换个条件试试"提示

#### 4.2.5 产品卡片

> 参考组件：SpotlightCard 发光追踪

| 状态 | 视觉表现 |
|------|---------|
| 默认 | 深色卡片背景，产品图 + 名称 + 分类 + 标签 |
| Hover | SpotlightCard光晕追踪鼠标位置，边框发光，轻微上浮 |
| Loading | 骨架屏（Skeleton） |
| 已收藏 | 右上角收藏图标实心高亮 |

**功能点**：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 产品信息展示 | P0 | 图片、名称、分类、标签 |
| 点击跳转详情 | P0 | 点击卡片任意区域跳转 |
| 收藏 | P1 | 右上角收藏图标 |
| 快速询价 | P1 | hover时显示"立即询价"按钮 |

---

### 4.3 产品详情页

#### 4.3.1 页面布局

```
┌───────────────────────────────────────────────────┐
│ 面包屑导航                                         │
├───────────────────┬───────────────────────────────┤
│ 图片画廊           │ 产品信息区                     │
│ ┌───────────────┐ │ · 产品标签                     │
│ │               │ │ · 产品名称                     │
│ │   主图        │ │ · 产品简介                     │
│ │               │ │ · 核心卖点列表                  │
│ └───────────────┘ │ · 价格区间（如有）               │
│ ┌──┐┌──┐┌──┐┌──┐ │ · "获取报价"按钮               │
│ │缩││缩││缩││缩│ │ · "加入收藏"按钮               │
│ │略││略││略││略│ │                               │
│ └──┘└──┘└──┘└──┘ │                               │
├───────────────────┴───────────────────────────────┤
│ 规格参数表（可折叠）                                │
├───────────────────────────────────────────────────┤
│ 产品特点（图标 + 标题 + 描述）                      │
├───────────────────────────────────────────────────┤
│ 应用场景                                           │
├───────────────────────────────────────────────────┤
│ 相关产品推荐                                       │
└───────────────────────────────────────────────────┘
```

#### 4.3.2 功能点

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 图片画廊 | P0 | 主图 + 缩略图，点击全屏查看，键盘/手势切换 |
| 产品信息展示 | P0 | 名称、标签、简介、卖点、价格 |
| 获取报价 | P0 | 打开询价表单弹窗 |
| 规格参数表 | P0 | 可折叠展开的参数表格 |
| 产品特点 | P1 | 图文展示核心卖点 |
| 应用场景 | P1 | 场景图片 + 场景名称 |
| 相关产品推荐 | P1 | 同类产品横向滚动 |
| 面包屑导航 | P0 | 首页 > 产品中心 > 分类 > 产品名 |

#### 4.3.3 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 产品名称 | string | 是 | H1标题 |
| 产品标签 | array | 否 | [热门/新品/推荐] Badge |
| 产品简介 | string | 是 | 2-3句话概述 |
| 核心卖点 | array | 否 | 图标 + 文字列表 |
| 价格区间 | string | 否 | 如"¥15,000 - ¥25,000" |
| 产品图片 | files | 是 | 多张，第1张为主图 |
| 规格参数 | json | 否 | `{dimensions: {width: "1200mm",...}, weight: "350kg",...}` |
| 产品特点 | array | 否 | [{icon, title, description}] |
| 应用场景 | array | 否 | [{scene_name, scene_image, description}] |

#### 4.3.4 交互规则

| 交互 | 行为 |
|------|------|
| 缩略图点击 | 切换主图，主图fade过渡 |
| 主图点击 | 打开全屏灯箱，支持左右切换、键盘控制、手势滑动 |
| 规格参数折叠 | 默认折叠，点击"展开参数"平滑展开 |
| 获取报价 | 弹出询价表单模态框，自动填入当前产品信息 |
| 加入收藏 | 添加到收藏列表，Toast 提示"已添加收藏" |
| 相关产品滚动 | 横向无限滚动，左右箭头控制 |
| 页面滚动 | 记录 `product_view` + `scroll_depth` 事件 |

---

### 4.4 场地设计页

#### 4.4.1 页面布局

```
┌───────────────────────────────────────────────────┐
│ 页面横幅："场地设计"                                │
├───────────────────────────────────────────────────┤
│ 场地案例列表                                       │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│ │  案例图片  │ │  案例图片  │ │  案例图片  │           │
│ │  标题      │ │  标题      │ │  标题      │           │
│ │  描述      │ │  描述      │ │  描述      │           │
│ └──────────┘ └──────────┘ └──────────┘           │
├───────────────────────────────────────────────────┤
│ 联系我们CTA                                        │
└───────────────────────────────────────────────────┘
```

#### 4.4.2 功能点

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 场地案例列表 | P0 | 图片 + 标题 + 描述网格 |
| 案例详情 | P1 | 点击查看案例详情页 |
| CTA | P0 | "免费获取设计方案"按钮 → 表单 |

---

### 4.5 关于我们页

#### 4.5.1 页面布局

```
┌───────────────────────────────────────────────────┐
│ 页面横幅："关于Loson科技"                            │
├───────────────────────────────────────────────────┤
│ 企业简介（左文右图）                                 │
├───────────────────────────────────────────────────┤
│ 发展历程时间轴                                      │
├───────────────────────────────────────────────────┤
│ 核心团队                                           │
├───────────────────────────────────────────────────┤
│ 荣誉资质                                           │
├───────────────────────────────────────────────────┤
│ 工厂环境（图片画廊）                                 │
└───────────────────────────────────────────────────┘
```

#### 4.5.2 功能点

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 企业简介 | P0 | 公司理念、使命、愿景，左右布局 |
| 发展历程 | P0 | 垂直时间轴，重要里程碑节点 |
| 团队展示 | P1 | 头像卡片 + 姓名职位 |
| 荣誉资质 | P1 | 证书图片网格，点击放大 |
| 工厂环境 | P1 | 图片画廊，生产车间/研发中心 |

---

### 4.6 联系我们页

#### 4.6.1 页面布局

```
┌───────────────────────────────────────────────────┐
│ 页面横幅 + 联系方式概览                              │
├───────────────────┬───────────────────────────────┤
│ 联系信息           │ 联系表单                       │
│ · 销售热线         │ · 姓名                        │
│ · 技术支持         │ · 电话                        │
│ · 电子邮箱         │ · 邮箱                        │
│ · 公司地址         │ · 公司                        │
│                   │ · 留言                        │
│                   │ · [提交]                       │
├───────────────────┴───────────────────────────────┤
│ 地图位置                                           │
└───────────────────────────────────────────────────┘
```

#### 4.6.2 功能点与字段

同首页表单模块（4.1.10），额外增加：

| 功能点 | 优先级 | 说明 |
|--------|--------|------|
| 联系信息展示 | P0 | 电话、邮箱、地址、工作时间 |
| 地图嵌入 | P1 | 百度/Google地图 |
| 表单提交 | P0 | 同首页表单 |

---

## 第5章 组件清单

### 5.1 布局组件

| 组件名 | 文件路径 | 说明 | 状态 |
|--------|---------|------|------|
| `Layout` | `components/layout/layout.tsx` | 全局布局容器 | 默认/滚动后 |
| `Navbar` | `components/layout/header/header.tsx` | 顶部导航栏 | 默认/滚动/移动端 |
| `MobileMenu` | `components/layout/header/mobile-menu.tsx` | 移动端汉堡菜单 | 打开/关闭 |
| `NavDropdown` | `components/layout/header/nav-dropdown.tsx` | 多级下拉导航 | 展开/折叠 |
| `Footer` | `components/layout/footer/footer.tsx` | 页脚 | - |
| `Breadcrumb` | `components/layout/breadcrumb.tsx` | 面包屑导航 | - |
| `Container` | `components/layout/container.tsx` | 内容容器（max-width + padding） | - |
| `Section` | `components/layout/section.tsx` | 页面区块容器 | - |

### 5.2 导航交互组件

| 组件名 | 说明 | 状态 | 参考组件 |
|--------|------|------|---------|
| `GlowMenu` | 导航菜单项3D翻转+发光效果 | 默认/hover翻转/active | glow-menu.tsx |
| `MegaDropdown` | 产品中心多级下拉菜单 | 关闭/展开/hover子项 | - |
| `SingleDropdown` | 关于我们单级下拉 | 关闭/展开 | - |
| `SearchOverlay` | 全屏搜索覆盖层 | 关闭/打开/搜索中 | Google AI Studio搜索框 |
| `FavoriteDrawer` | 收藏列表侧边抽屉 | 关闭/打开 | - |
| `FloatingActions` | 左侧固定收藏+搜索图标 | 默认/hover发光 | - |

### 5.3 首页业务组件

| 组件名 | 说明 | 状态 | 参考组件 |
|--------|------|------|---------|
| `HeroSection` | Hero视频背景区 | 加载中/播放中/视差滚动 | - |
| `CategoryGrid` | 热门产品分类不规则网格 | 默认/hover展开 | gourmet-grid HTML |
| `FeaturedProducts` | 热门产品卡片网格 | 加载/展示/空状态 | SpotlightCard |
| `ExpandingCards` | 为什么选择我们展开卡片 | 收缩/展开/动画中 | ExpandingCards |
| `ClientReviews` | 客户评论左右布局 | 评论切换/视频播放/头像轮播 | 客户评论HTML |
| `VenueDesignShowcase` | 场地设计展示 | 默认/hover | - |
| `SuitablePlaces` | 产品适合什么地方 | 默认/hover | - |
| `PurchaseProcess` | 采购流程步骤 | 入场动画/步骤高亮 | - |
| `ContactForm` | 首页联系表单 | 默认/验证错误/提交中/成功 | confetti+弹跳 |

### 5.4 产品相关组件

| 组件名 | 说明 | 状态 |
|--------|------|------|
| `ProductCard` | 产品卡片（SpotlightCard封装） | 默认/hover发光/loading/已收藏 |
| `ProductGrid` | 产品网格容器 | 加载/展示/空结果 |
| `ProductFilter` | 筛选侧栏 | 展开/折叠/已选标签 |
| `ProductSorter` | 排序下拉 | 展开/已选 |
| `ImageGallery` | 产品图片画廊 | 主图/缩略图/灯箱全屏 |
| `SpecTable` | 规格参数表 | 折叠/展开 |
| `FeaturesList` | 产品特点列表 | 入场动画 |
| `ApplicationScenes` | 应用场景展示 | 默认/hover |
| `RelatedProducts` | 相关产品横向滚动 | 滚动/左右箭头 |

### 5.5 通用UI组件

| 组件名 | 来源 | 状态 |
|--------|------|------|
| `Button` | shadcn/ui定制 | primary/secondary/outline/ghost/loading |
| `Input` | shadcn/ui定制 | 默认/聚焦(发光)/错误/禁用 |
| `Textarea` | shadcn/ui定制 | 默认/聚焦/错误 |
| `Select` | shadcn/ui定制 | 关闭/展开/已选 |
| `Dialog` | shadcn/ui | 关闭/打开/关闭中 |
| `Drawer` | shadcn/ui | 关闭/滑出/关闭中 |
| `Tabs` | shadcn/ui | 默认/激活 |
| `Badge` | shadcn/ui定制 | 热门(红)/新品(绿)/推荐(蓝) |
| `Avatar` | shadcn/ui | 默认/七彩描边 |
| `Skeleton` | shadcn/ui | 加载中 |
| `Toast` | shadcn/ui定制 | 成功/错误/信息/警告 |
| `Tooltip` | shadcn/ui | 默认/hover |

### 5.6 动效组件

| 组件名 | 说明 | 参考组件 |
|--------|------|---------|
| `SpotlightCard` | 鼠标位置光晕追踪发光效果 | SpotlightCard |
| `GlowMenu` | 3D翻转+发光菜单项 | glow-menu.tsx |
| `ConicGradientBorder` | conic-gradient旋转流光边框 | Google AI Studio搜索框 |
| `ConfettiEffect` | 表单提交成功彩纸飞舞 | confetti+弹跳 |
| `RainbowBorder` | 七彩描边动画（评论头像） | 客户评论HTML |
| `PageTransition` | 页面过渡动画 | Framer Motion |

### 5.7 用户追踪组件

| 组件名 | 说明 |
|--------|------|
| `TrackerProvider` | 追踪上下文Provider，管理visitor_id和session_id |
| `useVisitorId` | Hook：获取/生成visitor_id |
| `useTrackEvent` | Hook：上报行为事件 |
| `useScrollDepth` | Hook：跟踪滚动深度 |
| `TrackBuffer` | 缓冲区管理，定时/阈值/关键事件上报逻辑 |
| `useSourceTracker` | Hook：识别并记录来源渠道（referrer→source_type）+ UTM参数解析 |
| `useGeoInfo` | Hook：上报时区信息，IP地理解析由后端自动完成 |
| `usePageTracker` | Hook：页面级浏览追踪，自动记录进入/离开/停留时长/滚动深度 |
| `useJourneyTracker` | Hook：记录用户完整浏览路径（页面序列），用于后台路径分析 |

---

## 第6章 技术选型

### 6.1 技术栈总览

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **框架** | Next.js (App Router) | 14+ | SSR/SSG/ISR、路由、API Routes |
| **语言** | TypeScript | 5.x | 类型安全 |
| **样式** | Tailwind CSS | 3.4 | 原子化CSS、响应式、深色主题 |
| **UI组件** | shadcn/ui | latest | 基础UI组件（Button/Input/Dialog等） |
| **ORM** | Prisma | 5.x | 数据库操作、类型安全查询 |
| **数据库** | MySQL | 8.0 | 业务数据、用户行为数据存储 |
| **对象存储** | ali-oss | latest | 图片/视频文件存储 |
| **国际化** | next-intl | 3.x | 多语言支持（当前中文，预留英文） |
| **表单** | React Hook Form + Zod | latest | 表单管理 + 运行时验证 |
| **动画** | Framer Motion | 11.x | 页面过渡、微交互、3D翻转 |
| **富文本** | Tiptap | 2.x | 后台内容编辑器 |
| **图表** | Recharts | 2.x | 用户行为数据可视化 |
| **认证** | NextAuth.js | 4.x | 后台管理员认证（前台无登录） |

### 6.2 选型理由

| 技术 | 选型理由 |
|------|---------|
| Next.js 14+ App Router | RSC服务端组件优化首屏，App Router支持布局嵌套和流式渲染 |
| Tailwind CSS 3.4 | 深色主题天然支持，`dark:` 前缀，JIT模式零冗余 |
| shadcn/ui | 可定制性强，代码可读，不锁定组件库版本 |
| Prisma | 类型安全的数据库操作，与TypeScript完美配合 |
| ali-oss | 国内访问速度优，配合CDN加速图片/视频分发 |
| Framer Motion | React生态最成熟的动画库，支持3D变换和手势 |
| next-intl | Next.js官方推荐i18n方案，App Router原生支持 |

### 6.3 关键配置

#### next.config.js

```typescript
const nextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
    remotePatterns: [
      { protocol: 'https', hostname: '**.aliyuncs.com' },
    ],
  },
  experimental: {
    optimizeCss: true,
  },
}
```

#### tailwind.config.ts

```typescript
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        brand: {
          blue: '#3B82F6',
          purple: '#8B5CF6',
          green: '#22C55E',
          orange: '#F97316',
        },
        surface: {
          primary: '#0F172A',
          card: '#1E293B',
          hover: '#334155',
        },
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
      animation: {
        'glow-pulse': 'glow-pulse 2s ease-in-out infinite',
        'border-rotate': 'border-rotate 3s linear infinite',
      },
    },
  },
}
```

---

## 第7章 文件结构

```
src/
├── app/                                # Next.js App Router
│   ├── layout.tsx                      # 根布局
│   ├── template.tsx                    # 页面模板（过渡动画）
│   ├── error.tsx                       # 根错误边界
│   ├── not-found.tsx                   # 404页面
│   ├── loading.tsx                     # 全局加载状态
│   ├── globals.css                     # 全局样式
│   │
│   ├── (marketing)/                    # 营销页面组
│   │   ├── layout.tsx                  # 营销布局
│   │   ├── page.tsx                    # 首页
│   │   ├── about/page.tsx             # 关于我们
│   │   ├── contact/page.tsx           # 联系我们
│   │   ├── venue-design/page.tsx      # 场地设计
│   │   └── search/page.tsx            # 搜索结果
│   │
│   ├── products/                       # 产品页面
│   │   ├── page.tsx                    # 产品中心
│   │   ├── [category]/page.tsx        # 分类页
│   │   └── [slug]/page.tsx            # 产品详情
│   │
│   └── api/                           # API路由
│       ├── track/route.ts             # 用户行为上报
│       ├── inquiry/route.ts           # 询价提交
│       ├── revalidate/route.ts        # ISR重新验证
│       └── sitemap/route.ts           # 动态Sitemap
│
├── components/                         # 组件目录
│   ├── ui/                            # shadcn/ui基础组件
│   ├── layout/                        # 布局组件
│   │   ├── header/                    # 导航栏组件组
│   │   │   ├── header.tsx
│   │   │   ├── glow-menu.tsx         # 3D翻转+发光菜单 ★
│   │   │   ├── mega-dropdown.tsx     # 多级下拉
│   │   │   ├── mobile-menu.tsx
│   │   │   └── search-overlay.tsx    # 流光搜索覆盖层 ★
│   │   ├── footer/
│   │   └── floating-actions.tsx      # 左侧固定图标
│   │
│   ├── home/                          # 首页组件
│   │   ├── hero-section.tsx
│   │   ├── category-grid.tsx         # 不规则5列网格 ★
│   │   ├── featured-products.tsx
│   │   ├── expanding-cards.tsx       # 展开卡片 ★
│   │   ├── client-reviews.tsx        # 客户评论 ★
│   │   ├── venue-design-showcase.tsx
│   │   ├── suitable-places.tsx
│   │   ├── purchase-process.tsx
│   │   └── contact-form.tsx
│   │
│   ├── products/                      # 产品组件
│   │   ├── spotlight-card.tsx        # 发光追踪卡片 ★
│   │   ├── product-grid.tsx
│   │   ├── product-filter.tsx
│   │   ├── image-gallery.tsx
│   │   ├── spec-table.tsx
│   │   └── related-products.tsx
│   │
│   ├── effects/                       # 动效组件
│   │   ├── confetti-effect.tsx       # 彩纸飞舞 ★
│   │   ├── conic-border.tsx          # 流光边框 ★
│   │   ├── rainbow-border.tsx        # 七彩描边 ★
│   │   └── page-transition.tsx
│   │
│   └── tracking/                      # 用户追踪组件
│       ├── tracker-provider.tsx
│       ├── use-visitor-id.ts
│       ├── use-track-event.ts
│       └── track-buffer.ts
│
├── lib/                                # 工具库
│   ├── api/                           # API客户端
│   ├── hooks/                         # 自定义Hooks
│   ├── stores/                        # Zustand状态
│   ├── utils/                         # 工具函数
│   ├── validations/                   # Zod Schema
│   └── constants/                     # 常量
│
├── types/                              # TypeScript类型
│   ├── product.ts
│   ├── tracking.ts
│   └── inquiry.ts
│
├── prisma/                             # Prisma Schema
│   ├── schema.prisma
│   └── seed.ts
│
└── styles/                             # 全局样式
    └── globals.css
```

> 标注 ★ 的为参考组件需特别实现的文件

---

## 第8章 Mock数据说明

### 8.1 Mock数据策略

| 数据类型 | 开发阶段 | 生产阶段 |
|---------|---------|---------|
| 产品数据 | 本地JSON Mock | Prisma + MySQL |
| 用户行为数据 | 本地内存模拟 | MySQL tracking表 |
| 图片资源 | placeholder图片 | ali-oss CDN |
| 视频资源 | 示例视频URL | ali-oss CDN |

### 8.2 Mock数据文件结构

```
src/lib/mock/
├── products.ts              # 产品列表（12+条）
├── categories.ts            # 7大分类数据
├── reviews.ts               # 客户评论（6+条）
├── venues.ts                # 场地设计案例
├── team-members.ts          # 团队成员
├── settings.ts              # 全局配置
└── navigation.ts            # 导航菜单数据
```

### 8.3 关键Mock数据示例

#### categories.ts

```typescript
export const mockCategories = [
  {
    id: 'cat-1',
    name: '拳击机',
    slug: 'punching-machines',
    description: '专业级拳击竞技设备，精准力量检测',
    image: '/mock/punching.jpg',
    gridPosition: { col: '1', row: '1' },
    children: [
      { id: 'cat-1-1', name: '竞技型拳击机', slug: 'competitive', image: '/mock/punching-1.jpg' },
      { id: 'cat-1-2', name: '娱乐型拳击机', slug: 'entertainment', image: '/mock/punching-2.jpg' },
    ]
  },
  // ... 其余6大分类
]
```

#### products.ts

```typescript
export const mockProducts = Array.from({ length: 12 }, (_, i) => ({
  id: `prod-${i + 1}`,
  name: `超级娃娃机 X${i + 1}`,
  slug: `super-capsule-x${i + 1}`,
  category: '娃娃机',
  categoryId: 'cat-2',
  thumbnail: `/mock/product-${i + 1}.jpg`,
  shortDescription: '适合商场、游乐场的高品质娃娃机',
  tags: i % 3 === 0 ? ['hot'] : i % 3 === 1 ? ['new'] : [],
  isFeatured: i < 4,
  price: `${15000 + i * 2000}-${25000 + i * 3000}`,
  specs: {
    dimensions: { width: '1200mm', height: '2100mm', depth: '1500mm' },
    weight: '350kg',
    power: 'AC 220V / 500W',
    players: '1-2人',
  }
}))
```

### 8.4 用户追踪Mock

```typescript
// 开发环境模拟追踪行为
export const mockTrackingEvents = [
  { event: 'page_view', page: '/', duration: 45000 },
  { event: 'product_view', productId: 'prod-1', duration: 30000 },
  { event: 'category_browse', categoryId: 'cat-2', filterParams: {} },
  { event: 'search', keyword: '娃娃机', resultCount: 8 },
  { event: 'scroll_depth', page: '/products', depth: 75 },
]
```

---

## 第9章 验收标准

### 9.1 功能验收

| 验收项 | 验收标准 | 验收方法 |
|--------|---------|---------|
| **首页10个Section** | 所有Section内容正确渲染，无空白/错位 | 视觉走查 |
| **导航栏glow-menu** | 菜单项hover时3D翻转+发光效果正常 | 交互测试 |
| **导航栏多级下拉** | 产品中心下拉可展开3级，关于我们1级，数据正确 | 交互测试 |
| **热门分类不规则网格** | 5列不规则布局正确，hover展开说明文字 | 视觉+交互 |
| **SpotlightCard效果** | 产品卡片鼠标移动时光晕追踪，边框发光 | 交互测试 |
| **ExpandingCards** | 点击/hover展开卡片，其余收缩，动画流畅 | 交互测试 |
| **客户评论** | 左右布局正确，头像轮播+七彩描边动画，视频可播放 | 交互测试 |
| **搜索框流光** | conic-gradient旋转流光正常，聚焦时加速 | 视觉测试 |
| **表单验证** | 所有必填字段验证正确，错误提示清晰 | 表单测试 |
| **表单提交成功动画** | confetti彩纸飞舞+弹跳弹窗 | 交互测试 |
| **收藏功能** | 收藏图标可点击，收藏列表抽屉正确展示 | 功能测试 |
| **产品中心筛选** | 分类/价格/标签筛选正确，URL同步 | 功能测试 |
| **产品详情页** | 图片画廊、参数表、特点、场景、相关产品均正确 | 功能测试 |

### 9.2 用户身份追踪验收

| 验收项 | 验收标准 | 验收方法 |
|--------|---------|---------|
| **visitor_id生成** | 首次访问生成UUID，存入localStorage+Cookie+IndexedDB | DevTools检查 |
| **回访识别** | 关闭浏览器重新打开，visitor_id不变 | 浏览器测试 |
| **行为事件采集** | page_view/product_view/search/inquiry_submit事件正确触发 | Network面板检查 |
| **批量上报** | 30秒/10条阈值/关键事件即时，请求格式正确 | Network面板 |
| **页面关闭防丢** | 关闭浏览器/切换后台时sendBeacon成功发送 | Network面板 |
| **离线缓存** | 断网时事件存入IndexedDB，恢复后重试 | DevTools Offline模式 |
| **去重** | 同一event_id不会重复写入数据库 | 数据库检查 |
| **后台可视化** | UV/PV、产品热度、转化漏斗、搜索热词等看板正确展示 | 功能走查 |

**浏览记录模块验收**：

| 验收项 | 验收标准 | 测试方法 |
|--------|---------|---------|
| 页面浏览记录 | 每次页面访问都有完整记录（URL/来源/地域/设备/时长） | 后台浏览记录列表查看 |
| 产品浏览记录 | 产品详情页访问记录含产品ID、分类、浏览时长 | 后台产品浏览列表查看 |
| 来源识别 | direct/organic/social/referral/paid/email 6种类型正确识别 | 模拟不同来源访问验证 |
| UTM参数 | 5个UTM参数正确采集和存储 | URL加UTM参数访问验证 |
| 地域解析 | IP→国家/省/城市 正确解析（国内+海外） | 使用不同地区VPN测试 |
| 用户行为路径 | 页面间流转路径可可视化（桑基图） | 后台路径分析页面查看 |
| 转化漏斗 | 访问→产品浏览→询价 三级漏斗数据正确 | 完整走一遍流程验证 |
| 实时访客 | 5分钟内在线访客数据准确实时 | 多设备同时访问验证 |
| 数据保留 | 明细数据6个月自动归档 | 检查定时任务配置 |
| 隐私合规 | 不存储IP原始值，Cookie需用户同意 | 检查数据库和前端行为 |

### 9.3 响应式验收

| 验收项 | 验收标准 | 验收方法 |
|--------|---------|---------|
| **移动端 (<768px)** | 单列布局，汉堡菜单，文字可读，按钮可点击 | Chrome DevTools + 真机 |
| **平板端 (768-1023px)** | 2列网格，简化导航，间距合理 | Chrome DevTools |
| **桌面端 (≥1024px)** | 4列网格，完整导航，所有效果正常 | 桌面浏览器 |
| **不规则网格移动端** | 降级为2列常规网格，说明文字始终可见 | 真机测试 |

### 9.4 性能验收

| 指标 | 目标值 | 测量方法 |
|------|--------|---------|
| FCP (First Contentful Paint) | < 1.5s | Lighthouse |
| LCP (Largest Contentful Paint) | < 2.5s | Lighthouse |
| FID (First Input Delay) | < 100ms | Lighthouse |
| CLS (Cumulative Layout Shift) | < 0.1 | Lighthouse |
| TTI (Time to Interactive) | < 3.5s | Lighthouse |
| Lighthouse SEO评分 | > 90 | Lighthouse |
| Lighthouse Accessibility评分 | > 85 | Lighthouse |

### 9.5 SEO验收

| 验收项 | 验收标准 | 验收方法 |
|--------|---------|---------|
| Meta标签 | 每个页面title/description/keywords正确 | 查看源码 |
| 结构化数据 | 首页Organization、产品Product、面包屑BreadcrumbList | Google Rich Results Test |
| Sitemap | `/sitemap.xml` 可访问，包含所有页面 | 浏览器访问 |
| robots.txt | `/robots.txt` 正确配置，禁止/admin/和/api/ | 浏览器访问 |
| 图片alt | 所有图片均有alt属性 | 源码检查 |
| 语义化HTML | 正确使用header/main/section/nav/footer/article | 源码检查 |

### 9.6 无障碍验收

| 验收项 | 验收标准 |
|--------|---------|
| 焦点管理 | Tab键可按逻辑顺序遍历所有交互元素 |
| 键盘操作 | 所有功能可通过键盘完成（ESC关闭弹窗等） |
| ARIA标签 | 动态内容（下拉菜单、弹窗）有正确的aria属性 |
| 颜色对比度 | 文字与背景对比度 ≥ 4.5:1（WCAG 2.1 AA） |
| 表单标签 | 所有input有对应label |

### 9.7 兼容性验收

| 浏览器 | 版本要求 | 验收标准 |
|--------|---------|---------|
| Chrome | 90+ | 全功能正常 |
| Firefox | 90+ | 全功能正常 |
| Safari | 14+ | 全功能正常（含iOS Safari） |
| Edge | 90+ | 全功能正常 |

---

## 附录

### 附录A：参考组件索引

以下组件在文档中标注为"参考组件"，开发时需参考对应代码实现核心交互效果：

| 参考组件 | 应用位置 | 核心效果 |
|---------|---------|---------|
| glow-menu.tsx | 导航栏菜单项 | 3D CSS翻转（perspective + rotateY）+ 发光（box-shadow glow） |
| gourmet-grid HTML | 首页热门产品分类 | 不规则5列网格（grid-template-columns: 300fr 600fr 300fr 280fr 280fr） |
| ExpandingCards | 首页"为什么选择我们" | 卡片展开/收缩（flex比例动态调整 + transition） |
| 客户评论完整HTML | 首页客户评论 | 左右布局 + 底部头像轮播 + 七彩描边（conic-gradient border） |
| Google AI Studio搜索框 | 产品中心搜索框 | conic-gradient旋转流光边框（CSS @property --angle + animation） |
| SpotlightCard | 产品卡片 | 鼠标位置光晕追踪（onMouseMove + radial-gradient mask） |
| confetti+弹跳 | 表单提交成功 | canvas-confetti彩纸 + 弹性弹跳动画 |
| Alert/Toast | 全局通知 | shadcn/ui Toast封装，4种类型（success/error/info/warning） |

### 附录B：用户行为追踪数据库表设计

```sql
-- 访客表
CREATE TABLE visitors (
    id VARCHAR(36) PRIMARY KEY COMMENT 'visitor_id (UUID)',
    fingerprint VARCHAR(64) DEFAULT NULL COMMENT '浏览器指纹hash',
    user_agent TEXT COMMENT 'User-Agent',
    first_seen DATETIME DEFAULT CURRENT_TIMESTAMP,
    last_seen DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    total_visits INT DEFAULT 1,
    country VARCHAR(10) DEFAULT NULL,
    city VARCHAR(100) DEFAULT NULL,
    timezone VARCHAR(50) DEFAULT NULL COMMENT '时区',
    first_source_type VARCHAR(20) DEFAULT NULL COMMENT '首次访问来源类型',
    first_source_detail VARCHAR(200) DEFAULT NULL COMMENT '首次访问来源详情',
    first_utm_source VARCHAR(100) DEFAULT NULL COMMENT '首次访问UTM来源',
    KEY idx_visitors_fingerprint (fingerprint),
    KEY idx_visitors_last_seen (last_seen)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='访客表';

-- 会话表
CREATE TABLE sessions (
    id VARCHAR(36) PRIMARY KEY COMMENT 'session_id (UUID)',
    visitor_id VARCHAR(36) NOT NULL,
    started_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    ended_at DATETIME DEFAULT NULL,
    page_count INT DEFAULT 0,
    source VARCHAR(200) DEFAULT NULL COMMENT '来源（direct/search/social/referral）',
    utm_source VARCHAR(100) DEFAULT NULL COMMENT 'UTM来源',
    utm_medium VARCHAR(100) DEFAULT NULL COMMENT 'UTM媒介',
    utm_campaign VARCHAR(100) DEFAULT NULL COMMENT 'UTM活动',
    utm_term VARCHAR(100) DEFAULT NULL COMMENT 'UTM关键词',
    utm_content VARCHAR(100) DEFAULT NULL COMMENT 'UTM内容',
    country VARCHAR(10) DEFAULT NULL COMMENT '国家代码（ISO 3166-1 alpha-2）',
    region VARCHAR(100) DEFAULT NULL COMMENT '省/州',
    city VARCHAR(100) DEFAULT NULL COMMENT '城市',
    device_type VARCHAR(20) DEFAULT NULL COMMENT '设备类型：desktop/mobile/tablet',
    browser VARCHAR(50) DEFAULT NULL COMMENT '浏览器',
    os VARCHAR(50) DEFAULT NULL COMMENT '操作系统',
    KEY idx_sessions_visitor (visitor_id),
    KEY idx_sessions_started (started_at),
    CONSTRAINT fk_sessions_visitor FOREIGN KEY (visitor_id) REFERENCES visitors(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会话表';

-- 行为事件表
CREATE TABLE track_events (
    id VARCHAR(36) PRIMARY KEY COMMENT 'event_id (UUID)',
    visitor_id VARCHAR(36) NOT NULL,
    session_id VARCHAR(36) NOT NULL,
    event_type VARCHAR(50) NOT NULL COMMENT 'page_view/product_view/search/...',
    event_data JSON DEFAULT NULL COMMENT '事件详情JSON',
    page_url VARCHAR(500) DEFAULT NULL,
    referrer VARCHAR(500) DEFAULT NULL COMMENT '来源URL',
    source_type VARCHAR(20) DEFAULT NULL COMMENT '来源类型',
    country VARCHAR(10) DEFAULT NULL COMMENT '国家代码',
    duration INT DEFAULT NULL COMMENT '停留时长(秒)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_events_visitor (visitor_id),
    KEY idx_events_session (session_id),
    KEY idx_events_type (event_type),
    KEY idx_events_created (created_at),
    CONSTRAINT fk_events_visitor FOREIGN KEY (visitor_id) REFERENCES visitors(id) ON DELETE CASCADE,
    CONSTRAINT fk_events_session FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行为事件表';

-- 页面浏览记录表（用于后台浏览记录查询和地域分析）
CREATE TABLE page_views (
    id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
    visitor_id VARCHAR(36) NOT NULL COMMENT '访客ID',
    session_id VARCHAR(36) NOT NULL COMMENT '会话ID',
    page_url VARCHAR(500) NOT NULL COMMENT '页面URL路径',
    page_title VARCHAR(200) DEFAULT NULL COMMENT '页面标题',
    referrer VARCHAR(500) DEFAULT NULL COMMENT '来源URL',
    source_type VARCHAR(20) DEFAULT NULL COMMENT '来源类型：direct/organic/social/referral/paid/email',
    source_detail VARCHAR(200) DEFAULT NULL COMMENT '来源详情',
    utm_source VARCHAR(100) DEFAULT NULL COMMENT 'UTM来源',
    utm_medium VARCHAR(100) DEFAULT NULL COMMENT 'UTM媒介',
    utm_campaign VARCHAR(100) DEFAULT NULL COMMENT 'UTM活动',
    country VARCHAR(10) DEFAULT NULL COMMENT '国家代码',
    region VARCHAR(100) DEFAULT NULL COMMENT '省/州',
    city VARCHAR(100) DEFAULT NULL COMMENT '城市',
    device_type VARCHAR(20) DEFAULT NULL COMMENT '设备类型',
    browser VARCHAR(50) DEFAULT NULL COMMENT '浏览器',
    os VARCHAR(50) DEFAULT NULL COMMENT '操作系统',
    duration INT DEFAULT NULL COMMENT '停留时长(秒)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_pv_visitor (visitor_id),
    KEY idx_pv_session (session_id),
    KEY idx_pv_page (page_url),
    KEY idx_pv_source (source_type),
    KEY idx_pv_country (country),
    KEY idx_pv_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='页面浏览记录表';

-- 产品浏览记录表（用于产品浏览行为分析）
CREATE TABLE product_view_logs (
    id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
    visitor_id VARCHAR(36) NOT NULL COMMENT '访客ID',
    session_id VARCHAR(36) NOT NULL COMMENT '会话ID',
    product_id VARCHAR(36) NOT NULL COMMENT '产品ID',
    category_id VARCHAR(36) DEFAULT NULL COMMENT '产品分类ID',
    view_duration INT DEFAULT NULL COMMENT '浏览时长(秒)',
    from_page VARCHAR(500) DEFAULT NULL COMMENT '来源页面',
    country VARCHAR(10) DEFAULT NULL COMMENT '国家代码',
    city VARCHAR(100) DEFAULT NULL COMMENT '城市',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_pvl_visitor (visitor_id),
    KEY idx_pvl_product (product_id),
    KEY idx_pvl_category (category_id),
    KEY idx_pvl_country (country),
    KEY idx_pvl_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='产品浏览记录表';

-- 来源汇总日表（定时聚合，降低实时查询压力）
CREATE TABLE visitor_source_daily (
    id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
    stat_date DATE NOT NULL COMMENT '统计日期',
    source_type VARCHAR(20) NOT NULL COMMENT '来源类型',
    source_detail VARCHAR(200) DEFAULT NULL COMMENT '来源详情',
    country VARCHAR(10) DEFAULT NULL COMMENT '国家代码',
    uv_count INT NOT NULL DEFAULT 0 COMMENT '独立访客数',
    pv_count INT NOT NULL DEFAULT 0 COMMENT '页面浏览数',
    inquiry_count INT NOT NULL DEFAULT 0 COMMENT '询价数',
    avg_duration INT NOT NULL DEFAULT 0 COMMENT '平均停留时长(秒)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_source_daily (stat_date, source_type, source_detail, country),
    KEY idx_vsd_date (stat_date),
    KEY idx_vsd_source (source_type),
    KEY idx_vsd_country (country)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='访客来源日汇总表';

-- 地域统计日表
CREATE TABLE geo_daily (
    id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
    stat_date DATE NOT NULL COMMENT '统计日期',
    country VARCHAR(10) NOT NULL COMMENT '国家代码',
    region VARCHAR(100) DEFAULT NULL COMMENT '省/州',
    city VARCHAR(100) DEFAULT NULL COMMENT '城市',
    uv_count INT NOT NULL DEFAULT 0 COMMENT '独立访客数',
    pv_count INT NOT NULL DEFAULT 0 COMMENT '页面浏览数',
    inquiry_count INT NOT NULL DEFAULT 0 COMMENT '询价数',
    avg_duration INT NOT NULL DEFAULT 0 COMMENT '平均停留时长(秒)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_geo_daily (stat_date, country, region, city),
    KEY idx_gd_date (stat_date),
    KEY idx_gd_country (country)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='地域统计日表';
```

### 附录C：用户追踪API接口设计

```
POST /api/track
Content-Type: application/json

Request Body:
{
  "events": [
    {
      "event_id": "evt-uuid-1",
      "visitor_id": "visitor-uuid",
      "session_id": "session-uuid",
      "event_type": "page_view",
      "event_data": { "page_url": "/products", "referrer": "https://google.com" },
      "timestamp": "2025-01-15T10:30:00.000Z"
    }
  ]
}

Response:
{
  "success": true,
  "processed": 1,
  "duplicates": 0
}
```

**幂等保证**：后端通过 `event_id` 唯一索引实现去重，重复提交不会产生重复记录。

**增强的事件类型**：

| event_type | event_data 字段 | 说明 |
|-----------|-----------------|------|
| page_view | page_url, page_title, referrer, duration | 页面浏览 |
| product_view | product_id, category_id, view_duration, from_page | 产品浏览 |
| page_exit | page_url, duration, scroll_max_depth | 页面离开 |
| referrer_track | referrer_url, source_type, source_detail, utm_source, utm_medium, utm_campaign, utm_term, utm_content | 来源追踪 |
| search | keyword, result_count, filter_params | 搜索 |
| inquiry_submit | product_id, form_data(脱敏) | 询价提交 |
| favorite_add | product_id | 添加收藏 |
| scroll_depth | page_url, depth_percent | 滚动深度 |
| cta_click | cta_type, cta_location | CTA点击 |

**后台浏览记录查询接口**（需管理员认证）：

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 浏览概览 | GET | `/api/admin/analytics/overview` | UV/PV/询价/平均时长 |
| 访客趋势 | GET | `/api/admin/analytics/visitor-trend?days=30` | 近N天UV/PV |
| 来源分布 | GET | `/api/admin/analytics/source-distribution` | 各来源类型数据 |
| 地域分布 | GET | `/api/admin/analytics/geo-distribution` | 各国家数据 |
| 页面浏览列表 | GET | `/api/admin/analytics/page-views` | 分页浏览记录 |
| 产品浏览列表 | GET | `/api/admin/analytics/product-views` | 产品浏览汇总 |
| 用户路径 | GET | `/api/admin/analytics/journeys` | 桑基图路径数据 |
| 转化漏斗 | GET | `/api/admin/analytics/funnel` | 访问→浏览→询价 |
| 实时访客 | GET | `/api/admin/analytics/realtime` | 当前在线数据 |

**来源识别后端逻辑**（POST /api/track 接收到事件后）：
1. 提取请求 IP → GeoIP 解析 → 写入 country/region/city（不存IP）
2. 解析 referrer → 匹配来源规则 → 写入 source_type/source_detail
3. 解析 URL query → 提取 UTM 参数 → 写入 utm_* 字段
4. 批量写入 page_views 和 product_view_logs 表

### 附录D：CSS自定义属性（流光边框实现）

```css
@property --angle {
  syntax: '<angle>';
  initial-value: 0deg;
  inherits: false;
}

.conic-border {
  position: relative;
  border-radius: 12px;
  overflow: hidden;
}

.conic-border::before {
  content: '';
  position: absolute;
  inset: -2px;
  border-radius: inherit;
  background: conic-gradient(
    from var(--angle),
    #3B82F6, #8B5CF6, #EC4899, #3B82F6
  );
  animation: border-rotate 3s linear infinite;
  z-index: -1;
}

.conic-border::after {
  content: '';
  position: absolute;
  inset: 2px;
  border-radius: inherit;
  background: #1E293B;
  z-index: -1;
}

@keyframes border-rotate {
  to { --angle: 360deg; }
}
```

### 附录E：SpotlightCard核心逻辑伪代码

```typescript
// SpotlightCard: 鼠标位置光晕追踪
function SpotlightCard({ children, className }) {
  const cardRef = useRef<HTMLDivElement>(null)
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 })
  const [isHovered, setIsHovered] = useState(false)

  const handleMouseMove = (e: React.MouseEvent) => {
    const rect = cardRef.current?.getBoundingClientRect()
    if (rect) {
      setMousePosition({
        x: e.clientX - rect.left,
        y: e.clientY - rect.top,
      })
    }
  }

  return (
    <div
      ref={cardRef}
      onMouseMove={handleMouseMove}
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={() => setIsHovered(false)}
      className={cn(
        'relative rounded-2xl border border-white/5 bg-surface-card overflow-hidden',
        'transition-all duration-300',
        isHovered && 'border-blue-500/30 shadow-[0_0_30px_rgba(59,130,246,0.15)]',
        className
      )}
    >
      {/* 光晕层 */}
      {isHovered && (
        <div
          className="pointer-events-none absolute inset-0 z-10 transition-opacity duration-300"
          style={{
            background: `radial-gradient(600px circle at ${mousePosition.x}px ${mousePosition.y}px, rgba(59,130,246,0.1), transparent 40%)`,
          }}
        />
      )}
      {children}
    </div>
  )
}
```

---

**文档结束**

> **文档版本**: v1.0  
> **适用项目**: Loson科技官网前台  
> **技术栈**: Next.js 14+ / TypeScript / Tailwind CSS 3.4 / Prisma / MySQL  
> **设计风格**: 拟物化 + 科技感 + 深色 + 霓虹发光

---

> **版本更新说明**：v1.1 新增用户浏览记录模块，增强来源渠道追踪（6种来源类型+UTM参数采集）、地域分布分析（全球GeoIP解析）、用户行为路径可视化、实时访客监控等功能，完整支撑全球网站的访客行为分析需求。

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
